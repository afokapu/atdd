# Observer rule 14 — coach.observer.canonical-naming-drift
# Spec: atdd-coach-spec-v9.md §8.3.
# Implemented by detectors.correct_naming_drift (observer_rules/detectors.py).
# On drift, the rule's apply_correction() calls correct_naming_drift() which
# logs coach.session.canonical-session-name (session.convention.yaml).

trigger:
  type: python
  builder: "atdd.coach.observer_rules.canonical_naming_drift:build_rule"
