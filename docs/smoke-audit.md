# SMOKE Test Audit

Classification of `# Phase: SMOKE` acceptance tests against real-infrastructure criteria.

| acceptance-URN | entry-point-coverage | assertion-target | handoff-coverage | incident-cross-ref |
|---|---|---|---|---|
| acc:govern-providers:E004-SMOKE-001-real-lock-narrows-to-one-selected-rule | real (the toolkit's own committed .atdd/binding.lock.yaml) | rule selection narrows the bound set to exactly the named convention | single direction | #1674 (enforce rule selection; no synthetic substrate — asserts against the real lock) |
| acc:author-atdd-substrate:E009-SMOKE-001-real-repo-declares-no-review-rules | real (the toolkit's own committed .atdd/config.yaml) | the repo declares no author_review rules, so authoring stays unguarded | single direction | #1674 (pre-write review seam; guards the agnosticism claim — red here means the toolkit opted itself in) |
| acc:observe-and-correct:E008-SMOKE-001-delivery-waits-for-tui | synthetic (Python slow-start subprocess) | TUI ready-marker gate timing | single direction | #862; flagged for retrofit per #855 (synthetic-fixture-bypass + timing-flaky risk) |
| acc:observe-and-correct:E007-SMOKE-001-sentinel-enables-tui-submission | synthetic (Python echo-on-enter subprocess in raw termios) | submit sentinel (CR) delivery | single direction | #862; flagged for retrofit per #855 (synthetic-fixture-bypass) |
| acc:observe-and-correct:E003-SMOKE-001 | atdd-shim CLI subprocess | output.log contains CORRECTION_RECEIVED | dispatcher.dispatch → cli-return.jsonl → shim → agent stdin | #862 (rewritten from PersonaShim direct) |
| acc:observe-and-correct:E003-SMOKE-002 | atdd-shim CLI subprocess (`python -m atdd.coach.shim`) | captured stdout contains STDOUT_SENTINEL_E003_SMOKE_002 | shim pty → sys.stdout.buffer → operator-visible terminal | #843 (stdout forwarding) |
| acc:observe-and-correct:E004-SMOKE-001 | atdd spawn / cmd_spawn | shim is surface foreground process (ppid check), output.log grows | cmd_spawn → PersonaShim (via atdd-shim) → agent pty | #841 (spawn dispatch wiring) |
| acc:govern-lifecycle:E036-SMOKE-001-installed-shim-blocks-and-forwards-in-real-worktree | real (on-disk .atdd/bin/git installed in a real `git init` repo, real system git downstream, real shell PATH resolution) | block exits 1 + shared core.bare not poisoned; real `git status` forwards exit 0 | shell PATH → .atdd/bin/git shim → real git binary | #884 (agent-agnostic git-config bare guard; real binary, no synthetic stub) |
| acc:govern-lifecycle:C002-SMOKE-001-plan-tree-has-no-non-canonical-theme | real (validator scans the toolkit's own plan/ tree under REPO_ROOT) | zero non-canonical wagon themes reported (commons/plan/test/code/coach only) | plan/*/_*.yaml theme: declarations → check_must_be_canonical | #970 (canonical theme taxonomy; plan-time validator, no synthetic fixture) |
| acc:govern-lifecycle:C003-SMOKE-001-plan-tree-respects-boundary | real (validator scans the toolkit's own plan/ + src/ trees under REPO_ROOT) | zero unsuppressed commons-wagon-imports-coach violations | commons wagon src import scan → check_commons_coach_boundary | #970 (commons/coach boundary; plan-time validator, no synthetic fixture) |
| acc:govern-lifecycle:C004-SMOKE-001-plan-tree-urn-prefixes-align | real (validator scans the toolkit's own plan/ tree under REPO_ROOT) | zero unsuppressed produced-URN theme-prefix mismatches (commons:decision:* carve-out deferred to #951) | wagon produce[].name theme-prefix vs theme: → check_urn_namespace_matches | #970 (URN-namespace alignment; plan-time validator, no synthetic fixture) |
| acc:govern-lifecycle:C005-SMOKE-001-plan-tree-archetype-themes-align | real (validator scans the toolkit's own plan/ + src/ trees under REPO_ROOT) | zero plan/test/code wagons living outside their planner/tester/coder archetype root | wagon theme: vs archetype source root → check_archetype_alignment | #970 (archetype alignment; plan-time validator, no synthetic fixture) |
| acc:govern-lifecycle:C006-SMOKE-001-repo-config-keeps-commons-floor | real (validator resolves the toolkit's own .atdd/config.yaml under REPO_ROOT) | zero theme-zero-mandatory violations; resolved set pins commons at digit 0 | .atdd/config.yaml themes: block → resolve_theme_set → check_theme_zero_mandatory | #970 (commons mandatory floor; plan-time validator, no synthetic fixture) |
| acc:govern-lifecycle:E014-SMOKE-002-runtime-shim-entry-refuses-forbidden-flag | real (`python -m atdd.runtime.agent_control` module CLI as a subprocess; no mocks/FakeMultiplexer/stub) | non-zero exit + stderr names `--dangerously-skip-permissions` + no agents/<id> dir created (no process launched) | N/A (single component — the launch boundary refuses before any handoff) | #969 (retire forbidden flag + close E014 guard gap in cli-return transport; real subprocess, operator-observable refusal) |
| acc:govern-lifecycle:E043-SMOKE-001-live-cmux-native-launch-feed-no-shim | real (live cmux 0.64.10 + claude worker via the production launch builders; no mocks/stub) | worker boots with NO `atdd.coach.shim`/PersonaShim in path; positional prompt auto-submits (feed `userPrompt`+`stop`); activity publishes to `feed.list` with `source=claude` | build_agent_seed_argv + build_cmux_launch_argv → `cmux new-workspace --command` → claude positional seed → cmux wrapper hooks → `cmux rpc feed.list` | #978 (live verified 2026-06-05; no shim, no synthetic fixture — evidence below) |
| acc:consolidate-coach-workspace:C001-SMOKE-001-live-never-collapse | real (N>=2 live cmux-native per-workspace claude workers; no mocks) | after the layout pass, `surface.list` per worker-workspace yields exactly one worker identity — no worker migrated into another worker's or the coach's workspace (no daemon-scope collapse) | per-workspace workers → conformance pass (geometry over per-workspace surfaces) → cmux rpc surface.list per workspace | #865 (headline never-collapse safety; reproduces the #1013 _resolve_scope union hazard at N-worker scale; skips cleanly when cmux absent) |
| acc:consolidate-coach-workspace:E003-SMOKE-001-meta-smoke-print-theater-guard | real (live conformance pass driven through a recording multiplexer fake that captures every backend call) | a backend layout primitive invocation is observed; a layout-target print with zero layout primitive calls FAILS the smoke (catches print-theater regressions) | apply_canonical_name_and_layout shim → apply-layout use case → MultiplexerLayoutPort (recording) → recorded call log | #865 (meta-smoke guarding the core log-theater bug; asserts the real invocation, not the print) |
| acc:consolidate-coach-workspace:E004-SMOKE-001-validator-over-live-surface-names | real (the bound naming validator evaluated over a real cmux `surface.list`) | a role-aware managed surface name passes; a name lacking the role segment is flagged for re-application | cmux rpc surface.list → surface_state events → bound canonical-role-name validator | #865 (naming half of the stop-the-log-theater enforcement; skips cleanly when cmux absent) |
| acc:drive-state-machine:E002-SMOKE-001-rerun-after-no-marker-spawn-reattempts-current | real (a real issue warm-resumed at RED in a /tmp runtime dir; the RED persona's spawn rigged to fail before writing done.json so no current-phase completion marker is recorded; atdd coach <N> re-run) | the re-run re-spawns the RED persona (tester), NOT a GREEN coder; the atdd:RED label is unchanged; no earlier-persona agent_done replay advances the phase | atdd coach <N> → warm-resume completion-gate (current-phase done.json marker) → re-spawn-current vs advance-next | #1055 (the live #1051 RED-skip defect: a transient cmux Broken-pipe RED-spawn failure left RED with no done.json, and warm-resume + the unguarded agent_done branch kept advancing RED→GREEN on every re-run; skips cleanly when coach runtime absent) |
| acc:drive-state-machine:E007-SMOKE-001-is-registered-resolves-from-store-after-create | real (a real on-disk control-root State Store in /tmp holding a work item + github issue external_ref, with NO .atdd/manifest.yaml; the branch↔worktree binding written via `_record_binding_in_store`) | `IssueManager(root).branch_is_registered(branch)` returns True resolved from the store alone — proving the #1324 pre-commit gate resolves a freshly created worktree with no manifest present | `atdd worktree create` → `_record_binding_in_store` (data.branch/worktree_path) → `atdd issue is-registered` store-first read | #1347 (the store-side seam #1270's manifest decommission relies on; hermetic tmp store, no network) |
| acc:drive-state-machine:E008-SMOKE-001-create-makes-zero-commits-on-main | real (a real git repo in /tmp with an origin remote and a registered issue, local main at origin/main; `atdd worktree create <N>` executed) | local main's HEAD commit count is unchanged after create (zero new commits on main) and the new worktree exists based on origin/<default> | `atdd worktree create` → origin/<default> start_point + `_ff_sync_default_branch` + store binding (no on-main commit) | #1347 (ends the recurring unpushable local-main divergence; hermetic tmp repo, no network) |
| acc:drive-state-machine:D003-SMOKE-001-shipped-provider-table-resolves-in-a-real-process | real (the provider table as SHIPPED beside the installed package — not a fixture — imported by a separate process) | the table is present, parses, yields complete rows, resolves a session from its own env var, and resolves an empty environment to None without raising | installed package → `agent_session_env.yaml` → `load_provider_table` → `resolve_session` | #1540 (guards the silent-packaging miss: every unit test monkeypatches a fixture table, so none would notice the real file failing to ship — capture fails closed and a machine simply looks agent-free) |
| acc:drive-state-machine:E009-SMOKE-001-real-cli-mint-records-the-creating-session | real (the actual `atdd author issue` CLI as a subprocess against a real on-disk control-root State Store, with a stub `gh` on PATH — no network, no issue minted) | the real CLI path writes the session external_ref, the recency stamp, and the creator relationship | `atdd author issue` (subprocess) → `publish_issue` → `record_creator` → store | #1540 (the in-process unit proves the publish path calls the recorder but not that the real entry point does; the first GREEN of this feature shipped a recorder nothing called, and only an end-to-end invocation makes that visible) |
| acc:drive-state-machine:M004-SMOKE-001-recency-ordering-survives-a-real-store-reopen | real (a real on-disk store written and closed by one process, read back by a SEPARATE process) | ordering by `last_seen_at` is unchanged across the reopen, with the first-created session ranking first because it was seen last | `record_participation` → sqlite on disk → new process → `sessions_for_work_item` | #1540 (durability is the premise: the reader is an orchestrator whose recording process has crashed, so an ordering holding only in the writer's connection would be useless exactly when needed) |
| acc:drive-state-machine:E010-SMOKE-001-survives-no-verify-and-never-blocks-the-commit | real (a real git repo in /tmp with the packaged post-commit dispatcher installed and a real on-disk control-root State Store; commits made via a real `git commit --no-verify` subprocess) | the `--no-verify` commit still records the session participation (proving post-commit is NOT skipped the way pre-commit is), and a commit whose hook exits non-zero still succeeds with the commit object present | `git commit --no-verify` → packaged post-commit → `atdd state sessions capture` → `capture_post_commit` → store | #1540 (Decision 2 asserted against real git rather than trusted: agents use `--no-verify` routinely, so a pre-commit capture would miss exactly the commits it most needs; hermetic tmp repo, no network) |
| acc:drive-state-machine:L003-SMOKE-001-resume-command-is-runnable-after-recovery | real (a real on-disk control-root State Store in /tmp, recorded through the real capture path against the SHIPPED provider table, with a worktree path containing a space) | the emitted command names the recorded session id and worktree, carries no unrendered placeholders, and survives a `shlex` round-trip with the spaced path as ONE token | `capture_post_commit` → `sessions_for_work_item` → provider `resume_template` render | #1540 (the recovery payoff: store-only, no multiplexer installed; the spaced-path case caught a real unquoted-template defect during RED) |
| acc:version-source-of-truth:LIVE-SMOKE-001-installed-metadata-version-matches-store-bump | real (real on-disk Control Root + State Store; a REAL `pip wheel` build via the in-tree PEP 517 backend (`build_meta_shim/atdd_version_backend.py`) under build isolation; a REAL `pip install --no-deps` of that wheel into an isolated `python -m venv`; no mocks, no skip) | `importlib.metadata.version("atdd")` in the installed venv == the store-bumped version (4.1.1 after MAJOR+MINOR+PATCH), asserted distinct from both the seed (3.149.0) and the `0.0.0+local` no-store fallback — the load-bearing leg #1269 left as a gap | store `release` object → `state/version.py` bump → build backend resolver (reads store via `ATDD_CONTROL_ROOT`) → `pip wheel` → `pip install` → `importlib.metadata.version` | #1281 (closes the #1269 gap: no committed test asserted `importlib.metadata.version("atdd")` after a real build; anchored to `src/atdd/state/tests/test_version_live_smoke.py`, cannot self-skip; live verified 2026-07-01 — wheel `atdd-4.1.1-py3-none-any.whl`, installed metadata == 4.1.1) |
| acc:coach-ops:M003-SMOKE-001-installed-gh-accepts-the-query | real (the real `gh` binary against the live GitHub API; nothing substituted — no monkeypatch, no recorded payload, no wrapper over `subprocess`) | `gh` returns no `Unknown JSON field` refusal, the read yields a real verdict rather than `unknown`, and gh's advertised field set still matches the set the unit sibling records | `fetch_ci_status` → real `gh pr checks --json name,bucket,state` → verdict | #1612 (live-verified 2026-07-30, evidence below; the field list is read from gh at run time, never hardcoded) |
| acc:coach-ops:R001-SMOKE-001-real-gh-error-ends-the-wait-on-the-first-read | real (the shipped `wait_for_ci` driving the real `gh` binary against the live GitHub API; `sleep`/`clock` injected as observers only, nothing about `gh` stubbed) | zero sleeps on the first read, non-timeout status, and gh's own `Could not resolve` stderr in the reported detail | `wait_for_ci` → `fetch_ci_status` → real `gh pr checks` → live GraphQL error → terminal `ci_error` | #1612 (live-verified 2026-07-30, evidence below; retargeted at RED from a CLI-level test that could not reach the CI read — `cascade()` halts in `update_branch` first — see `revision_reason` in `plan/coach_ops/R001.yaml`) |

## coach-ops M003 / R001 live evidence (2026-07-30)

Captured against real `gh` 2.96.0 and the live GitHub API, driving the worktree
source (`PYTHONPATH=src`) at the **production** poll settings (30s interval,
1800s timeout). Anchored tests: 6/6 passed, zero skips.

```
=== shipped wait_for_ci, real gh, production 30s/1800s settings ===
real PR, checks red     pr=1650      status=ci_failed  sleeps=0  elapsed=1.32s
                        detail=failed: validate-gate (FAILURE), validate-gate (FAILURE)
PR that does not exist  pr=99999999  status=ci_error   sleeps=0  elapsed=0.44s
                        detail=CI read failed: GraphQL: Could not resolve to a
                               PullRequest with the number of 99999999. (repository.pullRequest)

=== fetch_ci_status across 12 real PRs ===
  #1649 MERGED -> pass  2 check(s) passed        #1667 OPEN -> fail  validate-gate (FAILURE)
  #1645 MERGED -> pass  2 check(s) passed        #1650 OPEN -> fail  validate-gate (FAILURE) x2
  #1633 MERGED -> pass  2 check(s) passed        #1651 OPEN -> fail  validate-gate (FAILURE)
  verdict classes observed: pass, fail
```

Both halves of the issue's Done-when, against real infrastructure: a green PR
reads `pass` (so a cascade would merge it) and a `gh` refusal is reported as a
real error in under half a second instead of spinning. Before the fix every one
of those twelve PRs read `unknown`, and both `wait_for_ci` drives slept 245-255
times before reporting `no CI result after 1800s`.

**Measurement note, recorded because it nearly produced a false negative.** The
first evidence run used a bare `python -c`, which imports `atdd` from the
installed pipx venv (4.30.0) — not the worktree. It faithfully reproduced the
OLD behaviour and would have read as "the fix does not work". The anchored tests
were never affected: they live inside the package, so pytest's rootdir insertion
resolves `atdd` to `src/`. Verified explicitly with an in-package import probe
before trusting either result. Any future live check here must set
`PYTHONPATH=src`, or it is measuring the last release.

## E043-SMOKE-001 live evidence (2026-06-05)

Captured by running the smoke against real cmux 0.64.10 + claude inside a cmux
session (`PYTHONPATH=src <venv> -m pytest -s
src/atdd/coach/commands/tests/test_e043_smoke_001_live_cmux_native_launch.py`):

```
workspace_ref : workspace:84
worker_cwd    : /private/var/folders/.../atdd-e043-smoke-42345
launch_argv   : ['cmux', 'new-workspace', '--name', 'atdd-e043-smoke-42345',
                 '--cwd', '.../atdd-e043-smoke-42345', '--command',
                 "claude 'Reply with exactly this token and nothing else: SMOKE-OK-978. Do not use any tools.' --permission-mode acceptEdits --allowedTools Read"]
feed_kinds    : ['sessionStart', 'stop', 'userPrompt']
turn_item     : {"kind": "stop", "source": "claude", "status": "telemetry",
                 "cwd": ".../atdd-e043-smoke-42345",
                 "id": "75329F80-03B9-4BAB-B18C-4C278737419C",
                 "workstream_id": "claude-ac70674f-0e3e-4b16-8465-28e452d3727c",
                 "created_at": "2026-06-05T20:34:38Z"}
shim_in_path  : []   (expected: [])
```

Proves all three acceptance criteria jointly: (1) `cmux new-workspace --command`
booted a claude worker with **no shim** in the launch path (`shim_in_path: []`,
and the `--command` string is a bare `claude` invocation); (2) the **positional
prompt auto-submitted** — the Feed recorded a `userPrompt` and a `stop` (the
worker took and finished its first turn unattended, no paste/sentinel); (3) the
worker's activity **published to `feed.list` with `source=claude`**, i.e. the
cmux wrapper's Feed hooks fired **without** the shim. `sessionStart` alone is
explicitly rejected by the smoke (would prove only boot, not auto-submit), so the
green is not a #855-style fake.

## Histogram

Breakdown of SMOKE acceptance rows by structural-bypass cause.

| cause | count |
|---|---|
| entry-point-coverage (synthetic stub, not real CLI) | 1 (E003-SMOKE-001 — retrofitted in #862) |
| synthetic-fixture (FakeMultiplexer / _SYNTHETIC_AGENT) | 0 (suppressed or retrofitted) |
| producer-only assertion (no consumer-side check) | 0 |
| handoff-gap (producer→consumer not in one test) | 0 |

## Future Tracking

Post-SMOKE regression metric — tracks bugs discovered after SMOKE passed CI.

| release-wave | post-SMOKE-bugs | expectation |
|---|---|---|
| v3.83.x | 2 (startup race #862, no-submit #862) | 0 |
| v3.84.x | TBD | 0 |
# SMOKE Acceptance Audit

**Created:** 2026-05-25
**Issue:** #855 — Investigate Smoke Test Systemic False Greens
**WMBT:** `wmbt:govern-lifecycle:E027`

Four production bugs shipped through green-SMOKE in the 2026-05-21 → 2026-05-24 session.
Each SMOKE test was technically passing — the assertion it made was true under the fixture
conditions it set up. Those conditions were not representative of production. This document
classifies every `phase: SMOKE` acceptance by structural cause and records the histogram
of bypass patterns.

---

## Classification Table

| acceptance-URN | entry-point-coverage | assertion-target | handoff-coverage | incident-cross-ref |
|---|---|---|---|---|
| acc:reconcile-local-store:C002-SMOKE-001-gitignored-projection-does-not-wipe-a-populated-store | real (throwaway git checkout + real .atdd/state/state.sqlite; no mocks, no patching) | a populated store survives a HEAD move against a gitignored, absent projection — reconcile refuses and every work_item is retained | single direction (reconcile refuses; no downstream handoff) | #1580 — reproduces the 2026-07-20 silent mass-deletion of ~588 work_items |
| acc:observe-and-correct:E006-SMOKE-001-stdin-bytes-reach-wrapped-subprocess | synthetic (cat subprocess) | stdin bytes round-trip | single direction | #861; flagged for retrofit per #855 (synthetic-fixture-bypass) |
| acc:consolidate-coach-workspace:D001-SMOKE-001-real-layout-holds-as-workers-added | real (atdd spawn + cmux) | layout persistence | N/A (UI state) | — |
| acc:consolidate-coach-workspace:E001-SMOKE-001-real-coach-tab-shows-every-issue | real (atdd spawn) | cmux pane list | N/A (UI state) | — |
| acc:consolidate-coach-workspace:E002-SMOKE-001-real-spawn-yields-one-tab-no-obs | real (atdd spawn) | pane count | N/A (UI state) | — |
| acc:consolidate-coach-workspace:Y001-SMOKE-001-real-multiplexer-shows-one-coach-tab | real (atdd spawn + cmux) | coach-tab presence | N/A (UI state) | — |
| acc:consolidate-coach-workspace:Y002-SMOKE-001-real-spawn-yields-one-tab | real (atdd spawn) | pane count | N/A (UI state) | — |
| acc:dispatch-ux-defaults-and-primer:E001-SMOKE-001-pane-mode-in-real-cmux-session | real (atdd spawn + cmux) | pane mode selection | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:E002-SMOKE-001-no-prompt-auto-in-piped-invocation | real (atdd spawn piped) | prompt suppression | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:E003-SMOKE-001-coach-invoked-from-worktree-parent | real (atdd coach) | invocation path | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:E004-SMOKE-001-worktree-reused-not-duplicated | real (atdd coach) | worktree reuse | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:E005-SMOKE-001-primer-printed-in-real-cmux-session | real (atdd coach + cmux) | primer output | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:E006-SMOKE-001-no-orphan-pane-after-spawn-failure | real (atdd spawn) | pane cleanup | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:E007-SMOKE-001-new-surface-in-pane-no-broken-pipe | real (atdd spawn) | pipe handling | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:Y001-SMOKE-001-help-text-shows-timeout-in-real-shell | real (atdd CLI) | help text content | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:Y002-SMOKE-001-banner-absent-after-sync-in-real-env | real (atdd sync) | banner suppression | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:Y003-SMOKE-001-resume-absent-from-real-help-output | real (atdd CLI) | help text content | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:Y004-SMOKE-001-real-template-render-excludes-sibling-deps-from-merge-wait | real (atdd session-template) | rendered output | N/A (single component) | — |
| acc:dispatch-ux-defaults-and-primer:Y004-UNIT-007-planner-validator-flags-bare-deps | real (atdd validate planner) | violation detection | N/A (validator) | — |
| acc:govern-lifecycle:D018-SMOKE-001-jel-app-repro-and-allowlist-round-trip | real (atdd CLI) | allowlist round-trip | both ends (write + read) | — |
| acc:govern-lifecycle:E003-SMOKE-001-real-validator-suite-includes-this-validator | real (atdd validate) | validator discovery | N/A (meta-validator) | — |
| acc:govern-lifecycle:E003-SMOKE-002-rules-grep-finds-both-new-rules | real (atdd rules grep) | rule discovery | N/A (meta-validator) | — |
| acc:govern-lifecycle:E003-SMOKE-003-rules-show-resolves-each-rule | real (atdd rules show) | rule resolution | N/A (meta-validator) | — |
| acc:govern-lifecycle:E004-SMOKE-001-rule-blocks-empty-graph-context | real (atdd validate coach) | rule enforcement | N/A (meta-validator) | — |
| acc:govern-lifecycle:E005-SMOKE-001-real-validate-coach-runs-extended-drift-validator | real (atdd validate coach) | validator execution | N/A (meta-validator) | — |
| acc:govern-lifecycle:E006-SMOKE-001-real-tester-suite-runs-both-validators | real (atdd validate tester) | validator discovery | N/A (meta-validator) | — |
| acc:govern-lifecycle:E006-SMOKE-002-rules-resolve-against-real-registry | real (atdd rules) | rule resolution | N/A (meta-validator) | — |
| acc:govern-lifecycle:E055-SMOKE-001-real-tester-suite-runs-live-smoke-validator | real (atdd validate tester) | live-smoke validator discovery + executes (not skipped) + rule resolves from rebuilt registry | N/A (meta-validator) | #1151 |
| acc:govern-lifecycle:E060-SMOKE-001-real-tester-suite-runs-constant-evidence-gate | real (collect_constant_evidence_violations over the live plan/ live_smoke acceptances R002/D002) | the gate resolves each live_smoke acceptance's anchored test to ≥1 real harness whose source exists, and returns zero violations (shipped harnesses return computed evidence, not a constant dict) — asserts non-vacuous resolution before asserting clean | live_smoke acceptance → scan_test_acceptance_headers → anchored test → invoked harness source → AST constant-evidence check | #1298 (extends #1151; catches the Y002 theater afokapu/atdd#1192); Part of afokapu/atdd-extensions#14 |
| acc:govern-lifecycle:E057-SMOKE-001-real-tester-suite-runs-phase-aware-binding-green | real (atdd validate tester) | binding validator over the live plan/ reports zero violations after the phase-aware refinement + the phase-aware test module is collected and runs (not skipped) | N/A (meta-validator) | #1242 |
| acc:govern-lifecycle:E061-SMOKE-001-real-cli-create-by-slug-warns | real (python -m atdd issue <slug> --dry-run subprocess, ATDD_DRY_RUN=1) | exit 0 + stderr names `atdd author issue` as the replacement + the warning is on stderr not the stdout body payload (the deprecated create-by-slug surface is signposted end-to-end) | atdd issue <slug> CLI dispatch → _deprecation_warning(stderr) | #1349 (atdd issue decommission; prerequisite #1272) |
| acc:govern-lifecycle:E058-SMOKE-001-publish-workflow-resolves-real-version-and-invokes-drain-double-gated | real (the real `atdd state version set/bump/emit` CLI over a tmp git repo with an annotated tag + a State Store; publish.yml parsed) | the CLI sequence resolves a real version (not 0.0.0+local) leaving a pending version_decided message; publish.yml reconciles current from `git describe --tags`, invokes the extension drain, is double-gated (dry_run + ATDD_RELEASE_ALLOW_PUBLISH) and idempotent via `git describe --exact-match HEAD` | reconcile (git describe) → `atdd state version set` → `bump` → version_decided → release-extension drain (tag + PyPI + external_ref) | #1285 (dry-run proof; real publish stays behind the double-gate; install-source pending ext#36) |
| acc:govern-lifecycle:E059-SMOKE-001-publish-job-configures-git-identity-so-annotated-tag-creation-succeeds | real (publish.yml parsed; real `git tag -a` run over a tmp git repo with and without a configured identity) | publish.yml sets both git `user.name` and `user.email` in the tag-release job before the drain step; an annotated `git tag -a` fails with an empty-identity error when unconfigured and succeeds once the release-bot identity is set | publish.yml git-config identity step → extension `git tag -a` (real git over a tmp repo) | #1297 (repro of run 28495533869's `empty ident name` tag failure; core sets the identity, the tag/publish mechanics stay the extension's) |
| acc:govern-lifecycle:E062-SMOKE-001-built-wheel-carries-every-non-py-source-file | real (`python -m build --wheel` over the repo checkout, in an isolated PEP-517 build env; the resulting wheel opened and its member list read) | every non-.py source file under src/atdd/ that is not on the exclude-package-data deny-list is present in the wheel (missing-set empty, vs 174 absent today); every .py module still ships (the deny-list suppresses .py as *data*, not as a module); no `__pycache__`/`*.pyc`/`.DS_Store` member appears | pyproject `[tool.setuptools.package-data]` + `[exclude-package-data]` → setuptools build_py → wheel members | #663, #952, #1369, packaging half of #1341 (all four resolve to this one declaration) |
| acc:govern-lifecycle:E064-SMOKE-001-built-wheel-ships-no-javascript | real (`python -m build --wheel` from a pristine copy that excludes `build/`, via the same `_wheel_harness` E062 uses; the resulting wheel opened and its member list read) | no wheel member ends in `.ts/.tsx/.js/.jsx/.mjs/.cjs` (vs 35 shipping today) except an exact-path allowlist; the allowlist covers only the two locations whose blockers are documented, so a new file cannot buy an exemption; the wheel's `Requires-Dist` declares no tree-sitter grammar | pyproject `[project] dependencies` + the broad `"atdd" = ["**/*"]` package-data glob → setuptools build_py → wheel members and METADATA | #1518 (fault-injected both ways: a stray `.ts` reddens it, and so does allowlisting one) |
| acc:govern-lifecycle:C009-SMOKE-001-consumer-sweep-collects-with-zero-errors-off-the-installed-wheel | real (wheel built from the repo, `pip install`ed into a freshly created venv — no editable install, no PYTHONPATH=src; pytest collection run from a `git init`'d synthetic consumer dir with no src/atdd/) | all four phase sweeps collect with ZERO errors (vs 15 in tester + 15 in coder on the pre-fix wheel, from `RuleNotInRegistryError` at import); coder and tester collect strictly more tests than the pre-fix wheel, proving the erroring modules now import; the wheel-completeness gate passes against the installed package | wheel build → clean-venv install → consumer-cwd pytest collection of the shipped `<phase>/validators/` trees | #1369 (the collection-error signature); the `validate-consumer` CI job this acceptance pins is the missing oracle for #663/#952/#1369 |
| acc:govern-lifecycle:C008-SMOKE-001-repaired-gate-executes-against-a-real-wheel | real (wheel built from the checkout, `pip install`ed into a fresh venv; the SHIPPED `test_wheel_completeness` run by pytest with the toolkit checkout as cwd) | the gate PASSES **and did not SKIP** — `" passed"` in the output, not a skip. The C008 units call the gate's internals directly and so cannot detect that pytest never reaches them; for the whole life of #451 it skipped on editable install (how CI runs it) and again from a wheel (its `__file__` walk found no checkout) | wheel build → clean-venv install → pytest over the installed validator, cwd=checkout → cwd source-tree discovery fallback | #451 (the gate that never executed an assertion in any environment); #1474 |
| acc:govern-lifecycle:E008-SMOKE-001-registration-visible-cross-worktree | real (atdd manifest) | cross-worktree visibility | both ends (write + read) | — |
| acc:govern-lifecycle:E009-SMOKE-001-real-validate-coach-runs-runtime-guard | real (atdd validate coach) | runtime guard | N/A (meta-validator) | — |
| acc:govern-lifecycle:E010-SMOKE-001-real-branch-creation-starts-at-origin | real (atdd branch) | branch origin | N/A (single component) | — |
| acc:govern-lifecycle:E011-SMOKE-001-real-fs-lock-prevents-concurrent-coaches | real (atdd coach) | fs lock enforcement | N/A (single component) | — |
| acc:govern-lifecycle:E012-SMOKE-001-pre-commit-hook-installed-allows-manifest-only | real (git commit) | hook behavior | N/A (single component) | — |
| acc:govern-lifecycle:E012-SMOKE-002-issue-reconcile-wired-in-cli | real (atdd issue reconcile) | CLI wiring | N/A (single component) | — |
| acc:govern-lifecycle:E013-SMOKE-001-live-gh-search-runs-without-error | real (gh search) | exit code | N/A (single component) | — |
| acc:govern-lifecycle:E014-SMOKE-001-spawn-guard-reachable-from-cli | real (atdd spawn) | guard reachability | N/A (single component) | — |
| acc:govern-lifecycle:E015-SMOKE-001-gate-output-contains-rules-in-live-repo | real (atdd gate) | output content | N/A (single component) | — |
| acc:govern-lifecycle:E016-SMOKE-001-backfill-on-real-fixture-suppresses-violations | real (atdd suppress backfill) | suppression effect | N/A (single component) | — |
| acc:govern-lifecycle:E017-SMOKE-001-docs-models-md-has-valid-structure | real (docs/MODELS.md) | file structure | N/A (single component) | — |
| acc:govern-lifecycle:E017-SMOKE-002-atdd-manifest-backfill-cli-wired | real (atdd manifest backfill) | CLI wiring | N/A (single component) | — |
| acc:govern-lifecycle:E018-INTEGRATION-001-scope-flag-in-cli-help | real (atdd validate --help) | help text | N/A (single component) | — |
| acc:govern-lifecycle:E018-INTEGRATION-002-scope-changed-files-exits-zero-on-clean-branch | real (atdd validate --scope changed-files) | exit code | N/A (single component) | — |
| acc:govern-lifecycle:E018-SMOKE-001-scoped-check-exits-zero-on-this-branch | real (atdd validate --scope changed-files) | exit code | N/A (single component) | — |
| acc:govern-lifecycle:E019-INTEGRATION-001-published-issue-passes-body-validators | real (atdd validate coach) | body validation | N/A (single component) | — |
| acc:govern-lifecycle:E019-SMOKE-001-live-create-zero-edit-calls | real (atdd issue) | gh API calls | N/A (single component) | — |
| acc:govern-lifecycle:E020-SMOKE-001-live-sync-codex-produces-conductor-md | real (atdd sync codex) | file output | N/A (single component) | — |
| acc:govern-lifecycle:E021-INTEGRATION-001-ci-check-fails-on-drifted-pr | real (CI check) | check result | N/A (single component) | — |
| acc:govern-lifecycle:E021-SMOKE-001-registry-check-passes-on-main-after-chore-pr | real (atdd validate) | exit code on main | N/A (single component) | — |
| acc:govern-lifecycle:E022-SMOKE-001-post-commit-leaves-core-bare-unchanged | real (git commit) | core.bare value | N/A (single component) | — |
| acc:govern-lifecycle:E023-SMOKE-001-routine-push-requires-zero-gate-bypasses | real (git push) | bypass count | N/A (single component) | — |
| acc:govern-lifecycle:E024-SMOKE-001-merge-produces-exactly-one-publish-success | real (gh merge) | publish event count | both ends (merge → publish) | — |
| acc:govern-lifecycle:E025-INTEGRATION-001-consumer-repo-sweep-excludes-custom-themes-tests | real (atdd validate tester) | scope exclusion | N/A (single component) | — |
| acc:govern-lifecycle:E025-SMOKE-001-consumer-validator-rejects-toolkit-only-tests | real (atdd validate tester) | violation detection | N/A (single component) | — |
| acc:govern-lifecycle:E026-SMOKE-001-routine-push-zero-bypasses-after-retirement | real (git push) | bypass count | N/A (single component) | — |
| acc:govern-lifecycle:E027-SMOKE-001-audit-covers-all-current-smoke-acceptances | real (plan/ YAML scan) | audit coverage | N/A (meta-validator) | #855 |
| acc:govern-lifecycle:E028-SMOKE-001-validate-planner-clean-after-retrofit | real (atdd validate planner CLI) | exit code 0 | N/A (meta-validator) | #855 |
| acc:govern-lifecycle:E029-SMOKE-001-retrofitted-smokes-pass-in-ci-without-bypasses | real (CI run, no bypass flags) | test suite result | both ends (post-retrofit) | #855 |
| acc:govern-lifecycle:E030-SMOKE-001-grep-returns-zero-matches | real (grep CLI on hook template files) | zero ATDD_SKIP_* matches | N/A (single component) | — |
| acc:govern-lifecycle:E031-SMOKE-001-emergency-cli-wired-in-atdd | real (python3 -m atdd.cli emergency) | EMERGENCY_BYPASS + audit-jsonl creation | N/A (single component) | — |
| acc:govern-lifecycle:E032-SMOKE-001-installed-shim-blocks-in-real-worktree | real (atdd init + gh issue create) | shim blocks command in live worktree | N/A (single component) | #816 |
| acc:govern-lifecycle:E033-SMOKE-001-real-commit-rejects-staged-create | real (git commit with pre-commit hook) | pre-commit rejects staged gh issue create | N/A (single component) | #816 |
| acc:govern-lifecycle:E063-SMOKE-001-real-commit-runs-changed-packaged-hook | real (git commit through core.hooksPath; the packaged hook changes with no refresh run) | the installed dispatcher execs the CHANGED packaged hook, so git blocks the commit — a control commit passes first, proving the block comes from the change and not a broken dispatcher | git -> core.hooksPath -> .atdd/hooks dispatcher -> `atdd hooks path` -> packaged hook | #1492 |
| acc:govern-lifecycle:E034-SMOKE-001-real-init-installs-and-warns | real (atdd init) | init installs shim + envrc entry | N/A (single component) | #816 |
| acc:govern-lifecycle:E035-SMOKE-001-real-import-purity-and-yaml-load | real (child interpreter subprocess imports atdd.coach.core + on-disk YAML load) | fresh import leaks no subprocess; phase_machine.convention.yaml parses to 9 phases | N/A (single component) | #888 |
| acc:govern-lifecycle:E037-SMOKE-001-real-collect-reports-emission | real (child interpreter subprocess runs the disposition-gate emit adapter writing an on-disk JSONL sink) | emitted ValidatorReport row carries the violation rule_id with disposition=block | N/A (single component) | #890 |
| acc:govern-lifecycle:E038-SMOKE-001-real-worktree-core-bare-false-on-disk | real (atdd.runtime.worktree.ensure_issue_worktree against a real on-disk git repo + real git CLI) | new worktree's per-worktree core.bare override reads false (I-9 canonical fix); shared config not left bare | N/A (single component) | #892 |
| acc:govern-lifecycle:E040-SMOKE-001-real-fs-run-roundtrip | real (two independently-constructed JsonlPersistenceStore instances over a real on-disk temp repo; real events.jsonl/decisions.jsonl/conventions.snapshot.yaml on disk) | a second store instance replays the identical on-disk event stream and load_run reconstructs an equal RunState — durability across instance boundaries | JsonlPersistenceStore(writer) → .atdd/runtime/runs/<id>/*.jsonl → JsonlPersistenceStore(reader) replay_events + load_run | #894 |
| acc:govern-lifecycle:E041-SMOKE-001-coach-end-to-end-via-trainrunner | real (coach.run cold-start against a real on-disk temp ATDD repo; real .atdd/runtime/runs/<id>/ scaffold + RunStarted event written to disk) | the per-issue drive is reached through JsonlTrainRunner.start_issue and a real run dir lands on disk; a repo grep finds no coach.commands.coach._drive_* private call outside the deprecated shim; runner_iface/issue_runner stay within §3.3 | coach.run → JsonlTrainRunner.start_issue → JsonlPersistenceStore.create_run (.atdd/runtime/runs/<id>/) → issue_runner.drive_single_issue | #895 |
| acc:govern-lifecycle:E042-SMOKE-001-jsonl-crash-recovery-identical-decisions | real (a real on-disk durable run is driven several phases in via the single-writer events.jsonl + frozen conventions.snapshot.yaml; a brand-new JsonlTrainRunner instance — the post-crash process with no in-memory state — replays it from disk via resume; GitHub evidence is a hermetic double so materialize_evidence is a pure function of the manifest + frozen snapshot) | the decision resume records equals the live loop's next coach.core.next_transition decision, and re-resuming on another fresh instance is identical (deterministic across process restart); resume writes no new PhaseAdvanced (no double-execution) — the §10.3 gate that governs any future Temporal adoption (§16 R-7) | kill -9 (discard runner) → JsonlTrainRunner(fresh).resume → load_conventions_for_run(frozen snapshot) + replay_events → materialize_evidence → coach.core.next_transition → RunResumed + decisions.jsonl | #896 |
| acc:govern-lifecycle:E044-SMOKE-001-toolkit-registry-build-leaves-no-stray-stub-dirs | real (the committed `.atdd/config.yaml` artifact run through the production `RegistryBuilder.build_all(mode="apply")`; no mocks/fakes) | the toolkit's shipped config declares only `code.toolkit: src/atdd`, and a real build leaves no `python/`/`supabase/`/`telemetry/` stub dirs | committed `.atdd/config.yaml` → RegistryBuilder.build_all → on-disk registry dirs | #984 (sibling of #970; reads the real shipped config, drives the real build) |
| acc:govern-lifecycle:L002-SMOKE-001-meta-walker-zero-hits-on-post-retrofit-repo | real (walk_all_smoke_acceptances_for_anti_patterns) | anti-pattern hits list | N/A (meta-validator) | #855 |
| acc:govern-lifecycle:R004-SMOKE-001-real-linked-worktree-recognized-worktree-ready | real (git worktree) | worktree detection | N/A (single component) | — |
| acc:govern-lifecycle:Y004-SMOKE-001-pre-commit-template-has-drift-notice | real (template file) | file content | N/A (single component) | — |
| acc:govern-lifecycle:Y005-SMOKE-001-reconcile-wired-in-cli | real (atdd issue reconcile) | CLI wiring | N/A (single component) | — |
| acc:govern-lifecycle:E045-SMOKE-001-real-subprocess-check-blocks-through-live-registry | real (real CommandGateCheck shells out to a real subprocess; live GateRegistry + evaluate_transition_gate; no mocks) | a genuinely-failing subprocess yields proceed=False (blocks), a genuinely-passing one yields proceed=True | CommandGateCheck → GateRegistry → evaluate_transition_gate → OS subprocess | #1020 (the enforcing-gate keystone; production wiring of the gate decision against a real process) |
| acc:govern-lifecycle:E050-SMOKE-001-real-cli-blocked-without-token-proceeds-after-coach-approve | real (real register_approval_checks → real ApprovalTokenGateCheck → real evaluate_transition_gate; the real `atdd coach approve` command writes a real signed token file under a real worktree; no mocks) | no token ⇒ proceed=False (gate blocks); after the real approve command writes the token ⇒ proceed=True; a PLANNED→RED token does not unlock RED→GREEN | register_approval_checks → GateRegistry → evaluate_transition_gate → ApprovalTokenGateCheck → filesystem token (Feed-decoupled) | #1017 (operator-approval enforcement; production wiring of the gate decision against the real token file the operator command produces) |
| acc:govern-lifecycle:E046-SMOKE-001-real-missing-tool-fails-closed | real (real CommandGateCheck names a binary absent from the host PATH; the OS raises FileNotFoundError) | a real missing tool is converted to a FAIL → proceed=False (fail-closed) | CommandGateCheck → GateRegistry → evaluate_transition_gate → OS exec attempt | #1020 (fail-closed guarantee verified against the real OS, not a stubbed exception) |
| acc:govern-lifecycle:E047-SMOKE-001-real-toolkit-scan-discovers-and-resolves | real (production config-driven composition analysis over the live src/atdd tree; toolkit's own shipped .atdd/config.yaml; no mocks/fixtures) | the #865 four-tier feature is analyzed and its composition-root-wired files carry no false unwired violation (atdd. edges resolved against the src import root) | .atdd/config.yaml code.toolkit → resolve_scan_roots → analyze_python_root (real src/atdd scan) | #958 (config-driving holds end-to-end; the #955 false-violation regression is fixed against the real tree) |
| acc:govern-lifecycle:E048-SMOKE-001-real-toolkit-files-discovered-over-live-tree | real (production boundaries finder over the live src/atdd tree; toolkit's own shipped .atdd/config.yaml; no mocks/fixtures) | a known real toolkit implementation file is discovered and the negative fixtures are excluded | .atdd/config.yaml code.toolkit → resolve_scan_roots → find_implementation_files (real src/atdd scan) | #958 (boundaries finder honors code.toolkit against the real tree) |
| acc:govern-lifecycle:E049-SMOKE-001-new-violation-beyond-baseline-fails-the-gate | real (collect_toolkit_violations runs the production config-driven composition analysis over the real src/atdd tree; real grandfathered baseline resolved from .atdd/baselines/) | a new violation absent from the baseline leaks (fails the gate) while pre-existing grandfathered debt stays absorbed | .atdd/config.yaml code.toolkit → resolve_scan_roots → analyze_python_root (real src/atdd scan) → new_violations vs .atdd/baselines/four_tier_toolkit.yaml | #958 (the four-tier ratchet bites on growth, not on legacy; real scan, no mocks) |
| acc:implement-code:D003-UNIT-002-no-false-positives | real (atdd validate) | no false violations | N/A (validator) | — |
| acc:integrate-end-to-end:E001-SMOKE-001-cycle-reaches-complete | real (full ATDD cycle) | issue status | both ends (INIT → COMPLETE) | — |
| acc:integrate-end-to-end:E001-SMOKE-002-artifacts-readable | real (full ATDD cycle) | artifact readability | both ends (write + read) | — |
| acc:integrate-end-to-end:M001-SMOKE-001-integration-log-covers-every-handoff | real (full ATDD cycle) | log completeness | both ends (all handoffs) | — |
| acc:integration-hardening:C002-SMOKE-001-pre-push-fires-on-real-git-push | real (git push) | hook execution | N/A (single component) | — |
| acc:integration-hardening:C002-SMOKE-002-commit-msg-fires-on-real-git-commit | real (git commit) | hook execution | N/A (single component) | — |
| acc:integration-hardening:C003-SMOKE-001-prepush-validator-fires-via-git-push | real (git push) | validator execution | N/A (single component) | — |
| acc:integration-hardening:C004-SMOKE-001-validator-via-real-gh-pr-list-against-repo | real (gh pr list) | validator execution | N/A (single component) | — |
| acc:integration-hardening:C005-SMOKE-001-coach-run-no-premature-advance-on-seeded-stale-done | real (atdd coach run) | state transition | N/A (single component) | — |
| acc:integration-hardening:E001-SMOKE-001-watcher-real-infrastructure | real (atdd coach watcher) | event delivery | both ends (emit + receive) | — |
| acc:integration-hardening:E006-SMOKE-001-hook-blocks-real-claude-invocation-against-installed-classifier | real (claude invocation) | hook block | N/A (single component) | — |
| acc:integration-hardening:E007-SMOKE-001-wave-plan-against-real-repo-graph | real (atdd coach wave planning — commands/wave_planning.py) | wave plan output | N/A (single component) | — |
| acc:integration-hardening:E008-SMOKE-001-nonexistent-pr-exits-0-with-broken-verdict | real (atdd review) | exit code | N/A (single component) | — |
| acc:integration-hardening:E009-SMOKE-001-resume-real-orchestration | real (atdd coach resume) | orchestration resume | N/A (single component) | — |
| acc:integration-hardening:M002-SMOKE-001-prepush-hook-fires-on-real-git-push | real (git push) | hook execution | N/A (single component) | — |
| acc:integration-hardening:Y001-SMOKE-001-handlers-importable-and-decisions-written-on-real-fs | real (fs write) | decisions persistence | both ends (write + read) | — |
| acc:integration-hardening:Y003-SMOKE-001-guard-catches-real-live-repo-contamination | real (atdd validate) | contamination detection | N/A (validator) | — |
| acc:integration-hardening:Y004-SMOKE-001-pre-push-hook-with-outdated-version-exits-1-cleanly | real (git push) | hook exit code | N/A (single component) | — |
| acc:integration-hardening:Y005-SMOKE-001-pipx-install-detection-end-to-end | real (atdd upgrade) | install detection | N/A (single component) | — |
| acc:integration-hardening:Y006-SMOKE-001-init-force-in-linked-worktree-writes-config-worktree-file | real (atdd init) | config file location | N/A (single component) | — |
| acc:integration-hardening:Y007-SMOKE-001-real-upgrade-with-stdin-closed-does-not-raise | real (`atdd upgrade --no-pypi` as a subprocess with stdin=DEVNULL, so stdin genuinely is not a terminal; nothing mocked in the child) | exit 0 and no EOFError in the captured output | N/A (single component — the reproduction is the process boundary itself) | #1628 (the field traceback at upgrader.py:56 that left every push in every worktree blocked until a human ran the command) |
| acc:integration-hardening:E008-SMOKE-001-two-real-concurrent-upgrades-serialise-on-one-install | real (two `atdd upgrade --no-pypi` subprocesses launched simultaneously from two real checkouts sharing one install; the real lock, unpatched) | non-overlapping mutating sections, both exit 0, install intact and stamped current | two checkouts → install-scoped lock → sync step per checkout | #1628 (~60 agents share one pipx install, so the answer to concurrency is a lock and idempotency rather than an operator) |
| acc:judge-ambiguous-decisions:D006-SMOKE-001-review-returns-parseable-verdict | real (atdd review) | verdict JSON | N/A (single component) | — |
| acc:observe-and-correct:E003-SMOKE-001-correction-loop-end-to-end | synthetic→real (retrofitted #855) | correction delivery via stdin | both ends (observer writes + shim delivers) | #824, #841, #854 |
| acc:observe-and-correct:E003-SMOKE-002-operator-stdout-visible | synthetic→real (retrofitted #855) | proc.stdout bytes | N/A (single component) | #843 |
| acc:observe-and-correct:E004-SMOKE-001-real-spawn-uses-shim-process-tree | real (atdd spawn via _ProcessLaunchingFakeMx) | process-tree parentage | partial (process spawned; cli-return consumer not verified) | #854 |
| acc:observe-and-correct:E005-SMOKE-001-full-shim-spawn-with-env-override | real (atdd-shim CLI) | env-var inheritance | N/A (single component) | — |
| acc:observe-and-correct:M005-SMOKE-001-real-observer-heartbeats-reach-coach | real (atdd observer + coach) | heartbeat delivery | both ends (emit + receive) | — |
| acc:observe-and-correct:M006-SMOKE-001-real-blocked-worker-reaches-coach | real (atdd observer + coach) | block event delivery | both ends (emit + receive) | — |
| acc:observe-and-correct:P001-SMOKE-002-observer-loop-closes | real (atdd observer CLI) | correction loop convergence | both ends (producer writes + convergence verified) | #825 |
| acc:observe-and-correct:P002-SMOKE-001-blocked-persona-triggers-correction | real (atdd spawn + observer) | correction trigger | both ends (drift → correction) | — |
| acc:observe-and-correct:R001-SMOKE-001-real-ask-answer-roundtrip | real (atdd observer + agent) | ask/answer protocol | both ends (ask + answer) | — |
| acc:consolidate-coach-workspace:E005-SMOKE-001-surface-workspace-resolves-without-invalid-handle | real (live cmux session + real worker surface in its own workspace) | surface_workspace returns a bare `workspace:N` handle with no `Invalid workspace handle` error; layout conformance applies | list-workspaces → handle sanitizer → list-panes → place_worker_surface_right | #1025 (layout handle parse bug; relates #601/#865) |
| acc:govern-lifecycle:E051-SMOKE-001-full-cycle-zero-projects-v2-graphql | real (IssueManager.update over a real manifest/config + recording GitHub client, no PROJECT_TOKEN) | a full RED→COMPLETE drive emits zero Projects-v2 board GraphQL; every transition is an `atdd:<phase>` label swap (REST) | issue.update → label REST swap; board methods asserted not-called | #1051 (decommission Projects v2; recording boundary verifies the negative — no board op is reachable) |
| acc:govern-lifecycle:E052-SMOKE-001-train-gate-enforces-offline-zero-board-graphql | real (Train gate over `.atdd/manifest.yaml` validated against `plan/_trains.yaml`) | the Train gate resolves the train from the manifest and enforces validity with zero `get_project_item_field_values` board read or fallback | manifest `_manifest_train` → validate vs plan/_trains.yaml | #1051 (offline train gate; board read deleted, no fallback) |
| acc:govern-lifecycle:E053-SMOKE-001-config-loads-and-github-imports-without-projects-v2 | real (live import of `atdd.integrations.github` + config load with no `project_id`) | `projects_v2` module is genuinely absent from shipped code; `integrations.github` exposes no projects_v2 attr; config loads cleanly without `project_id` | import surface + config loader (no board plumbing) | #1051 (substrate removal; real import/load, no mocks) |
| acc:govern-lifecycle:E054-SMOKE-001-issue-reconcile-runs-without-unboundlocalerror | real (`atdd issue reconcile` invoked through a real subprocess against the live CLI dispatch, no monkeypatching) | the reconcile command's combined output never contains `UnboundLocalError` or "cannot access local variable 'IssueManager'" — the module-level import is in effect on the reconcile path | real CLI entry → main() dispatch → `manager = IssueManager()` (module global) → reconcile() | #1056 (scoping-regression fix; real subprocess proves the constructor binds, not just source inspection) |
| acc:govern-lifecycle:R009-SMOKE-001-a-refused-transition-exits-red-with-a-diagnosis | real (`atdd coach transition` invoked as a real subprocess against a real `.atdd` store, with a `gh` on PATH that refuses label writes using GitHub's not-authorised wording) | the process exits non-zero, its output names the credential and the missing permission and says retrying cannot help, and it contains no Python traceback | real CLI entry → `IssueManager.update` → store write (#1452) → `_write_phase_label` → `GitHubClient._run_gh` refusal classification | #1621 (the refusal is the thing under test; a real subprocess proves the diagnosis reaches stdout rather than escaping as an exception) |
| acc:author-atdd-substrate:D001-SMOKE-001-schemas-load-and-validate | real (the four on-disk canonical schemas loaded + a known-good authored fixture per kind) | all four canonical schemas load from their flat home and each known-good fixture validates against its schema | canonical schema files → load_schema(kind) → jsonschema validate fixture | #1097 (planned; substrate-spine canonical schemas + flat homes) |
| acc:author-atdd-substrate:C001-SMOKE-001-cli-refuses-bad-input | real (installed `atdd author` CLI in a real checkout; invalid role/id/path) | the process exits non-zero with a validation error and no artifact file is created or modified | shell → atdd author spine → validate_author_input (role/id/path) | #1097 (planned; shared author spine input guard) |
| acc:author-atdd-substrate:P001-SMOKE-001-cli-extension-default-and-core-flag | real (installed `atdd author` CLI in a real checkout; --extension, --core, and neither) | --extension writes inside extensions/<id>/ and not into src/atdd/; --core writes the core path; neither flag exits non-zero without writing | shell → atdd author → resolve_context → context-aware home | #1097 (planned; extension-first context resolution) |
| acc:author-atdd-substrate:K001-SMOKE-001-authored-artifact-not-rejected | real (installed `atdd author` + `atdd validate` CLIs in a real checkout) | one artifact of each kind authored by the CLI is well-formed against its own schema and `atdd validate` exits clean — neither erroring on nor quarantining the new paths (no authoring bypass; no *consuming* validator exists yet) | atdd author write (each kind) → atdd validate (current suite) over new paths | #1097 (planned; authoring not rejected/regressed by existing gates) |
| acc:author-atdd-substrate:R001-SMOKE-001-merge-driver-resolves-concurrent-inserts | real (real git repo; `.gitattributes`-registered re-sort/dedup merge driver; two branches each inserting into the same role-block of a registry file) | a real `git merge` completes cleanly (no `<<<<<<<` markers) and the registry file is canonically sorted by key with no duplicate entry | two branches → git merge → re-sort/dedup merge driver (load→dedup→stable-sort on union) | #1097 (planned; deterministic registry merge driver) |
| acc:author-atdd-substrate:E001-SMOKE-001-cli-creates-valid-node | real (installed `atdd author convention-node` CLI in a real checkout) | a flat per-role file appears under src/atdd/<role>/conventions/nodes/ for the rule_id and validates against the convention-node schema | shell → atdd author convention-node → flat nodes/ writer | #1098 (planned; convention-node flat per-role file) |
| acc:author-atdd-substrate:C002-SMOKE-001-cli-rejects-bad-node | real (installed `atdd author convention-node` CLI; bad role / rule_id / subfolder / numbered term) | the process exits non-zero with a structural validation error and no node file is created under nodes/ | shell → atdd author convention-node → validate_convention_node guard | #1098 (planned; structurally-invalid convention-node rejection) |
| acc:author-atdd-substrate:E007-SMOKE-001-cli-writes-validation | real (installed `atdd author convention-node --validation` CLI in a real checkout) | a flat per-role node is written carrying the top-level `validation` block (registry-checked family/template, subject_kind=interlocking) and validates against the convention-node schema; a malformed `--validation` JSON exits non-zero and writes no node | shell → atdd author convention-node → _json_arg parse → validate_convention_node validation guard → flat nodes/ writer | #1247 (planned; optional convention-node validation metadata for interlocking rules) |
| acc:author-atdd-substrate:E002-SMOKE-001-two-edges-sorted-deduped | real (installed `atdd author relationship` CLI; two out-of-order edges plus one duplicate) | relationships.yaml contains both unique edges sorted by source_rule_id; the duplicate insert leaves the file unchanged | shell → atdd author relationship → dedup-insert + stable-sort + atomic write | #1099 (planned; relationships.yaml deterministic sorted-insert) |
| acc:author-atdd-substrate:C003-SMOKE-001-cli-rejects-bad-edge | real (installed `atdd author relationship` CLI; malformed edge) | the process exits non-zero with a validation error and relationships.yaml is unchanged on disk | shell → atdd author relationship → validate_edge guard | #1099 (planned; malformed relationship edge rejection) |
| acc:author-atdd-substrate:E003-SMOKE-001-scope-written-and-sorted | real (installed `atdd author scope` CLI; valid scope with one selector) | scopes.yaml gains the scope in scope_id-sorted position and the scope validates against the canonical scope schema | shell → atdd author scope → dedup-insert + stable-sort by scope_id + atomic write | #1100 (planned; scopes.yaml deterministic sorted-insert) |
| acc:author-atdd-substrate:C004-SMOKE-001-cli-rejects-bad-scope | real (installed `atdd author scope` CLI; invalid or empty scope) | the process exits non-zero with a validation error and scopes.yaml is unchanged on disk | shell → atdd author scope → validate_scope guard (>=1 selector) | #1100 (planned; malformed/empty scope rejection) |
| acc:author-atdd-substrate:E004-SMOKE-001-cli-writes-valid-gate | real (installed `atdd author gate` CLI; valid gate for a trigger) | the per-trigger file under src/atdd/coach/gates/ gains the gate in gate_id-sorted position and validates against the canonical gate schema | shell → atdd author gate → per-trigger dedup-insert + stable-sort by gate_id + atomic write | #1101 (planned; per-trigger gate file deterministic sorted-insert) |
| acc:author-atdd-substrate:C005-SMOKE-001-cli-rejects-bad-gate | real (installed `atdd author gate` CLI; malformed gate) | the process exits non-zero with a validation error and the per-trigger gate file is unchanged on disk | shell → atdd author gate → validate_gate guard (id/trigger/strategy/action/exit) | #1101 (planned; malformed gate rejection) |
| acc:author-atdd-substrate:P002-SMOKE-001-cli-init | real (installed `atdd author {extension,workspace} init` CLI in a real checkout) | workspace init writes workspaces/<id>/atdd.workspace.yaml and extension init writes extensions/<id>/atdd.extension.yaml; a reserved-publisher (atdd) workspace id exits non-zero without writing | shell → atdd author … init → validate id → scaffold manifest + skeleton | #1104 (planned; package scaffolders for the four-layer model) |
| acc:author-atdd-substrate:C006-SMOKE-001-cli-manifest-roundtrip | real (provider manifest scaffolded by the installed `atdd author workspace init` CLI) | the CLI-written workspace manifest passes validate_workspace_manifest; an implementation inside the provider's requires_contract is accepted and one outside it is refused | shell → atdd author workspace init → load manifest → validate_workspace_manifest + contract check | #1104 (planned; manifest validators + provider↔implementation contract) |

---

## Histogram — Structural Bypass Cause

| structural-cause | count | description |
|---|---|---|
| synthetic-fixture / entry-point-bypass | 2 | Test drives a synthetic subprocess or direct class instantiation instead of the real CLI entry point (pre-retrofit) |
| synthetic→real (retrofitted) | 2 | Originally synthetic-fixture; retrofitted to real entry point by #855 |
| producer-only / handoff-gap | 2 | Test asserts on the producer side (artifact written) without verifying the consumer received it |
| real-entry-point / partial-handoff | 1 | Test drives real spawn but does not verify the full handoff path |
| real-entry-point / full-coverage | 22 | Test drives real entry point and verifies operator-observable behavior end-to-end |
| real-entry-point / single-component | 64 | Test drives real CLI entry point; asserts on single-component outcome (no cross-component handoff to verify) |
| real-entry-point / meta-validator | 16 | Test validates validator tooling itself (atdd validate, atdd rules, atdd repo graph) |

**Total classified:** 116 SMOKE acceptances (as of 2026-05-28)

### Cause definitions

- **synthetic-fixture / entry-point-bypass**: SMOKE test instantiates a class directly (`PersonaShim(`)
  or calls a subprocess with a synthetic command (`cat`, `sleep`, embedded Python script) rather than
  routing through the real CLI entry point (`atdd spawn`, `atdd coach`). The production wiring —
  `_inject_agent_env`, `_build_shim_command`, adapter command construction — is bypassed entirely.
  Root cause of **#854** (Popen crash invisible to synthetic E003-SMOKE tests).

- **producer-only / handoff-gap**: SMOKE test asserts only on the producer side (e.g., artifact
  written to `output.log` or `cli-return.jsonl`). When the feature has a producer→consumer handoff,
  the consumer side is never exercised. Test passes green even when the consumer is completely
  unwired. Root cause of **#824** (cli-return consumer never wired, SMOKE green because it only
  checked that the producer wrote the file).

- **real-entry-point / partial-handoff**: SMOKE test uses the real CLI entry point but does not
  verify the full end-to-end handoff. The process tree is correct but the correction delivery
  to the agent's stdin is not asserted.

- **real-entry-point / full-coverage**: SMOKE test drives the real CLI entry point and asserts
  on operator-observable behavior at both ends of the handoff. This is the canonical shape.

---

## Lived Incident Cross-Reference

| incident | issue | SMOKE that should have caught it | structural cause | bypass mechanism |
|---|---|---|---|---|
| cli-return consumer not wired | #824 | acc:observe-and-correct:E003-SMOKE-001 | producer-only / handoff-gap | SMOKE asserted producer wrote cli-return.jsonl; no consumer existed |
| shim swallows pty output | #843 | acc:observe-and-correct:E003-SMOKE-002 | synthetic-fixture + producer-only | SMOKE asserted on output.log; never checked operator-visible stdout |
| RED tests in consumer wheel | #846 | (no relevant SMOKE; coverage gap) | missing SMOKE | No SMOKE for "consumer entry points don't surface RED tests" |
| Shim Popen crashes on real command | #854 | acc:observe-and-correct:E003-SMOKE-001/002 | synthetic-fixture / entry-point-bypass | E003-SMOKE used Python stub; _inject_agent_env + _build_shim_command never called |

---

## Future Tracking

Post-retrofit regression metric. Expectation: zero post-SMOKE production bugs per release wave
from v3.83.0 onwards. Any non-zero entry must reference the root-cause issue and the WMBT
whose SMOKE test failed to catch it.

| release-wave | post-SMOKE-bugs | expectation | notes |
|---|---|---|---|
| v3.83.x | 0 | 0 | Baseline established after E028/E029 retrofit (issue #855) |
| v3.84.x | — | 0 | (pending) |
| v3.85.x | — | 0 | (pending) |

**Maintenance rule:** When a production bug is discovered after a SMOKE phase passed green,
add a row here with `post-SMOKE-bugs > 0`, link the root-cause issue, and identify which
WMBT SMOKE acceptance failed to catch it. Then author a new WMBT (or extend existing) to
tighten the coverage.

---

## Remediation Plan

| phase | action | WMBT |
|---|---|---|
| E028 GREEN | Extend smoke.convention.yaml with 3 anti-pattern rules | wmbt:govern-lifecycle:E028 |
| E028 GREEN | Create planner-side validator test_smoke_synthetic_fixture_bypass.py | wmbt:govern-lifecycle:E028 |
| E029 GREEN | Retrofit test_e003_smoke_001 to use real atdd spawn path | wmbt:govern-lifecycle:E029 |
| E029 GREEN | Retrofit test_e003_smoke_002 to remove _SYNTHETIC_AGENT | wmbt:govern-lifecycle:E029 |
| E029 GREEN | Remove ATDD_RUN_SMOKE=1 opt-in gate from test_e004_smoke_001 | wmbt:govern-lifecycle:E029 |
| L002 GREEN | Implement walk_all_smoke_acceptances_for_anti_patterns meta-walker | wmbt:govern-lifecycle:L002 |
| acc:author-atdd-substrate:C007-SMOKE-001-validate-package-cli | real (subprocess `atdd validate package` against the installed toolkit + fixture/official package dir) | CLI exits 0, package valid against package-relatively-loaded core, no runtime executed | <path> -> discover_packages -> validate_by_kind -> validate_realizes -> compose_protocol_view | #1130/#1133 composition substrate |
| acc:author-atdd-substrate:E005-SMOKE-001-package-composes | real (fixture package set + atdd-extensions/official when checked out beside core) | discover + validate-by-kind + separate source graph + composed view with derived realization edges, no runtime executed | package dir -> discover -> compose_protocol_view(composed) | #1133 |
| acc:author-plan-substrate:E001-SMOKE-001-cli-creates-valid-wagon | real (installed `atdd author wagon` CLI in a real checkout) | a manifest appears at plan/<slug_snake>/_<slug_snake>.yaml and `atdd validate planner` accepts the created wagon without manual fixups | shell → atdd author wagon → plan/<slug>/ single-file writer → atdd validate planner | #1144 (planned; plan-kind wagon writer, on-Confirm path for atdd plan #1139) |
| acc:author-plan-substrate:C001-SMOKE-001-cli-refuses-bad-plan-input | real (installed `atdd author <plan-kind>` CLI in a real checkout; invalid slug/URN/step-code/path) | the process exits non-zero with a validation error and no artifact file under plan/ is created or modified | shell → atdd author <plan-kind> → validate_plan_author_input (slug/URN/step-code/path) | #1144 (planned; plan-artifact input guard) |
| acc:author-plan-substrate:C002-SMOKE-001-cli-refuses-invalid-wagon | real (installed `atdd author wagon` CLI in a real checkout; manifest missing a required field) | the process exits non-zero with a validation error and no manifest file is created under plan/ | shell → atdd author wagon → validate_wagon guard (required header + produce keys) | #1144 (planned; structurally-invalid wagon rejection) |
| acc:author-plan-substrate:E002-SMOKE-001-cli-creates-valid-feature | real (installed `atdd author feature` CLI in a real checkout) | a file appears at plan/<slug>/features/<name>.yaml and `atdd validate planner` accepts it without manual fixups | shell → atdd author feature → plan/<slug>/features/ single-file writer | #1144 (planned; plan-kind feature writer) |
| acc:author-plan-substrate:C003-SMOKE-001-cli-refuses-invalid-feature | real (installed `atdd author feature` CLI; feature missing a required field) | the process exits non-zero with a validation error and no feature file is created under plan/ | shell → atdd author feature → validate_feature guard | #1144 (planned; structurally-invalid feature rejection) |
| acc:author-plan-substrate:E003-SMOKE-001-cli-creates-valid-wmbt | real (installed `atdd author wmbt` CLI in a real checkout) | a file appears at plan/<slug>/<CODE>.yaml in ODI grammar with a SMOKE acceptance and `atdd validate planner` accepts it | shell → atdd author wmbt → plan/<slug>/ step-coded writer (seeds SMOKE) | #1144 (planned; plan-kind wmbt writer, must-have-smoke by construction) |
| acc:author-plan-substrate:C004-SMOKE-001-cli-refuses-invalid-wmbt | real (installed `atdd author wmbt` CLI; WMBT with no SMOKE acceptance) | the process exits non-zero with a validation error and no WMBT file is created under plan/ | shell → atdd author wmbt → validate_wmbt guard (ODI + statement-OOC + must-have-smoke) | #1144 (planned; structurally-invalid wmbt rejection) |
| acc:author-plan-substrate:E004-SMOKE-001-cli-creates-valid-train | real (installed `atdd author train` CLI in a real checkout) | _trains.yaml gains the entry and plan/_trains/<id>.yaml appears, both accepted by `atdd validate planner` | shell → atdd author train → _trains.yaml dedup-insert + per-train write | #1144 (planned; plan-kind train writer, registry-class) |
| acc:author-plan-substrate:C005-SMOKE-001-cli-refuses-invalid-train | real (installed `atdd author train` CLI; malformed train_id) | the process exits non-zero with a validation error and plan/_trains.yaml is unchanged on disk | shell → atdd author train → validate_train guard (id/uniqueness/path) | #1144 (planned; structurally-invalid train rejection) |
| acc:author-plan-substrate:E005-SMOKE-001-cli-appends-acceptance | real (installed `atdd author acceptance` CLI; existing WMBT file) | the target WMBT gains the acceptance under acceptances[] and `atdd validate planner` accepts the modified WMBT | shell → atdd author acceptance → in-place acceptances[] append (idempotent on urn) | #1144 (planned; plan-kind acceptance writer) |
| acc:author-plan-substrate:C006-SMOKE-001-cli-refuses-malformed-acceptance | real (installed `atdd author acceptance` CLI; non-existent target WMBT) | the process exits non-zero with a validation error and no WMBT file is created or modified | shell → atdd author acceptance → validate_acceptance guard (target/urn/phase) | #1144 (planned; malformed acceptance rejection) |
| acc:author-plan-substrate:E007-SMOKE-001-cli-authors-interlocking | real (installed `atdd author interlocking` CLI in a real checkout; route trains already authored on disk) | plan/_trains/_interlockings/<id>.yaml is written, loads via load_interlocking (schema-valid, digests present), and plan/_trains/_interlockings.yaml gains the deduped interlocking_id entry | shell → atdd author interlocking → stamp_interlocking_digests → pinned-order write + _interlockings.yaml dedup-insert | #1265 (planned; plan-kind interlocking writer, derives only digests) |
| acc:author-plan-substrate:E008-SMOKE-001-cli-authors-contract | real (`atdd author contract --spec` CLI in a real checkout via `python -m atdd`) | a schema file is written at the identity-derived contracts/{theme}/…/{aspect}.schema.json, validates as draft-07 and carries $id contract:{identity}, and contracts/_contracts.yaml gains the deduped, sorted entry | shell → atdd author contract → create_contract (validate identity → derive path → write draft-07 schema + $id → _contracts.yaml dedup-insert) | #1330 (planned; plan-kind contract writer, keystone of #1314 B) |
| acc:admit-substrate:D001-SMOKE-001-schemas-shipped | real (subprocess; substrate-lock schema resolved package-relatively from the installed atdd toolkit) | a real `.atdd/substrate.lock.yaml` validates against the installed substrate-lock schema, no repo checkout | lockfile -> schemas.validate_lock (package-data schema) | #1142 (admission front door; schemas ship as planner package data) |
| acc:admit-substrate:C001-SMOKE-001-add-does-not-execute | real (`atdd add` subprocess over a poisoned extension package) | `atdd add` admits/refuses without executing the poisoned implementation (sentinel never written, no POISONED error), and is a real command (not argparse reject) | atdd add -> resolve -> validate+compose (no implementation import) | #1142 (no-runtime-execution invariant; runtime binding is a later wagon) |
| acc:admit-substrate:L001-SMOKE-001-search-cli | real (`atdd search` subprocess over a configured local registry index) | the command prints the matching artifact id/kind/version and creates no install dir | atdd search -> _load_registry_entries -> registry.search | #1142 (admission front door; search locates, never installs) |
| acc:admit-substrate:C002-SMOKE-001-ambiguous-add-refused | real (`atdd add <ambiguous-alias>` subprocess) | exits non-zero, prints both candidate canonical ids, writes no lockfile | atdd add -> resolver.resolve -> AmbiguousAliasError | #1142 (alias is UX; canonical id is authoritative) |
| acc:admit-substrate:C003-SMOKE-001-add-validates-cli | real (`atdd add --path <invalid>` + `atdd validate package <valid>` subprocesses) | invalid (missing owned file) exits non-zero and installs nothing; validate package still exits zero | atdd add -> validate_and_compose (refuse) ; validate package unaffected | #1142 (admission validates before install; no regression) |
| acc:admit-substrate:E001-SMOKE-001-add-list-roundtrip | real (`atdd add --path <pkg>` then `atdd list --substrate` subprocesses) | add installs to versioned home + writes a digest-pinned lock; list renders the locked entry from the lockfile | atdd add -> installer.install + upsert_lock_entry ; atdd list --substrate -> read_lock | #1142 (content-addressed install; list reads the lock, not a scan) |
| acc:admit-substrate:C004-SMOKE-001-remove-cli | real (`atdd remove` subprocess over a seeded lock) | removing a depended-on workspace refuses + leaves the lock unchanged; removing a leaf extension succeeds + drops its lock entry | atdd remove -> admission.remove (dependency check) | #1142 (remove never strands a dependent) |
| acc:admit-substrate:E002-SMOKE-001-grouped-add-list-parity | real (`atdd substrate add --path <pkg>` then `atdd substrate list` subprocesses) | grouped add installs to the versioned home + writes a digest-pinned lock; grouped list renders the locked entry; exit codes + lock effects match the flat form | atdd substrate add -> run_add ; atdd substrate list -> run_list (same handlers as flat verbs) | #1239 (additive noun grouping; grouped routes to existing handlers) |
| acc:admit-substrate:C005-SMOKE-001-flat-verb-deprecation-warns | real (deprecated flat `atdd add --path <pkg>` subprocess) | add still exits zero + installs as before; stderr carries a deprecation notice naming `atdd substrate add`; the notice is absent from stdout | atdd add -> _deprecation_warning(stderr) -> run_add | #1239 (flat verbs deprecated-but-working until #1207/4.0.0) |
| acc:admit-substrate:L002-SMOKE-001-substrate-help-lists-five | real (`atdd substrate --help` + `atdd add --help` subprocesses) | substrate help lists add/remove/bind/capabilities/list; the flat `atdd add` help is marked [DEPRECATED] and points to the grouped form | atdd substrate --help -> subparser help ; atdd add --help -> deprecated help string | #1239 (grouped noun is the discoverable surface) |
| acc:admit-substrate:C006-SMOKE-001-readme-documents-grouped | real (subprocess greps the on-disk README.md) | the README references `atdd substrate` as the canonical management surface and presents no bare flat substrate verb as the primary form | README.md -> grep `atdd substrate` | #1239 (docs present the grouped form as canonical) |
| acc:bind-substrate-runtime:D001-SMOKE-001-schema-shipped | real (subprocess; binding-lock schema resolved package-relatively from the installed atdd toolkit) | a real `.atdd/binding.lock.yaml` from `atdd bind --check` validates against the installed binding-lock schema, no repo checkout, no implementation imported | binding plan -> validate against package-data binding-lock schema | #1157 (binding plan schema ships as planner package data) |
| acc:bind-substrate-runtime:L001-SMOKE-001-load-real-github | real (a real admitted github extension + python-pytest provider via `atdd add`) | the binder loads enabled packages from their installed_path and indexes the real github implementations by realizes_convention, importing no implementation code | real substrate.lock.yaml -> lock_loader -> index by realizes_convention | #1157 (load exactly the locked-and-enabled set) |
| acc:bind-substrate-runtime:C001-SMOKE-001-tamper-refused-subprocess | real (`atdd bind --check` subprocess over an admitted package whose installed_path was mutated after admission) | the command refuses the tampered package with a digest mismatch before any subprocess is spawned; exit non-zero / package marked refused | atdd bind --check -> recompute digest vs lock -> refuse | #1157 (bind is the first executor; tamper boundary) |
| acc:bind-substrate-runtime:C002-SMOKE-001-real-contract-compatible | real (`atdd bind --check` subprocess over the real github extension + python-pytest provider) | every shipped github implementation passes the SemVer contract check against the provider; the plan records them bound to python-pytest; no false contract-mismatch | atdd bind --check -> workspace_resolver SemVer check | #1157 (contract-compatible binding only) |
| acc:bind-substrate-runtime:E001-SMOKE-001-real-pytest-spawn | real (the real python-pytest provider provider-spawns a real github implementation as a pytest subprocess) | the implementation runs as a real child process; its Violation(s) are captured over the violation-output-contract; the implementation module is never imported into core | atdd bind exec -> provider-spawn (pytest subprocess) -> parse violations | #1157 (provider-spawn isolation; core imports no extension code) |
| acc:bind-substrate-runtime:E002-SMOKE-001-real-bound-gate-blocks | real (a real provider-spawned github implementation gating a real lifecycle transition) | the bound Violation is routed into the disposition gate, the legacy validator is shadowed (suppressed + logged), and the transition is blocked end to end | atdd bind exec -> violation_router (shadow legacy) -> disposition gate blocks | #1157 (bound impl gates like a legacy validator) |
| acc:bind-substrate-runtime:M001-SMOKE-001-real-crash-falls-back | real (`atdd bind --check` subprocess where a bound implementation is forced to crash under provider-spawn) | the binder detects the crash, falls back to the legacy validator for that convention, and emits a loud attributable error; the gate is never silently passed | atdd bind exec -> detect crash -> fall back to legacy + log loudly | #1157 (fail-safe to legacy, never fail-open) |
| acc:govern-providers:D001-SMOKE-001-real-vendored-provider-resolves-to-cli-path | real (the toolkit's own vendored substrate_home resolved by the enforce runner; no synthetic fixture) | resolve_provider for workspace_id atdd.workspace.python-pytest returns a ResolvedProvider whose provider_cli_path exists on disk and is a file — the live subprocess boundary | enforce.resolution.resolve_provider over real .atdd/workspaces candidate roots | #1425 (provider-boundary-model; subprocessed-not-imported, contract_version SemVer-refused) |
| acc:govern-providers:E001-SMOKE-001-conformance-runs-over-real-dual-tree-substrate | real (the toolkit's real committed .atdd/binding.lock.yaml + vendored workspaces + extensions trees) | conformance(repo_root) returns an (ok, report) pair without raising and the report names the bound-rule runnability count over the real lock, exercising the dual-tree (#1359) resolution live | enforce.runner.conformance -> _resolve_impls_root over both real trees | #1425 (extension-shipped-detector-resolution; absorbs #1359) |
| acc:govern-providers:E002-SMOKE-001-real-lock-surfaces-the-four-phantom-tester-detectors | real (the toolkit's real committed .atdd/binding.lock.yaml + vendored .atdd/extensions convention nodes) | find_orphan_detectors returns a set including the four known tester.* orphan convention_ids, each genuinely lacking an <id>.convention.yaml node | enforce.orphans.find_orphan_detectors -> real lock vs real convention-node scan | #1425 (orphan-detector-detection; the 4 phantom lock entries) |
| acc:govern-providers:E003-SMOKE-001-real-lock-has-no-extension-identity | real (the toolkit's real committed .atdd/binding.lock.yaml text) | the strings atdd.extension.coder and atdd.extension.tester each occur zero times; every bound entry is keyed by convention_id and implementation_id, not a package identity | read committed .atdd/binding.lock.yaml -> assert no package-identity leak | #1425 (binding-lock-identity-independence; keeps the persona rename #1343/#1344 a lock regeneration) |
| acc:govern-registry:D001-SMOKE-001-real-registry-roots-at-core-only | real (the toolkit's real src/atdd core convention tree + its vendored .atdd/extensions convention nodes) | no file admitted into the core rule registry lives under .atdd/extensions, and every real extension rule_id is already a member of the core rule_id set (the extension-only set is empty) — the core-only scope holds live | enforce.registry.core_rule_ids / extension_rule_ids over the real substrate | #1427 (registry-scope-decision; Path A stays core-only by design) |
| acc:govern-registry:E001-SMOKE-001-real-mirrors-all-resolve-to-live-core-rules | real (the toolkit's real src/atdd core convention tree + its vendored .atdd/extensions convention nodes) | find_mirror_incoherences returns an empty list — every vendored extension node's source.legacy_rule_id is a member of the live core rule_id set, so no mirror has drifted | enforce.registry.find_mirror_incoherences over the real core registry | #1427 (mirror-coherence-validation; the latent hole from the extension side) |
| acc:govern-registry:E002-SMOKE-001-real-merge-would-duplicate-every-mirror | real (the toolkit's real core rule_id set + its real extension rule_id set) | assert_core_precedes_extension raises DuplicateRuleError over the real registries, and the set of colliding rule_ids equals the full real extension rule_id set — admitting the mirrors would add only duplicates | enforce.registry.assert_core_precedes_extension over the real registries | #1427 (duplicate-rule-precedence; the concrete evidence behind core-only) |
| acc:guard-succession:E003-SMOKE-001-real-blocking-path-b-permits-real-core-succession | real (the toolkit's real enforce-extensions CI job + its real binding.lock + vendored extension nodes) | Path B is found BLOCKING (the enforce-extensions verdict carries neither continue-on-error nor \|\| true) and the succession guard PERMITS deleting a real bound-and-mirrored core rule — enforcement survives it — while still REFUSING one whose twin is unbound, so the guard is not hollowed out | enforce.registry.path_b_is_blocking + guard_core_deletion over the real substrate | #1427 (core-succession-guard) → updated by #1428 (E001 made Path B blocking; the guard and its WMBT are unchanged, the live evidence flipped) |
| acc:guard-succession:E003-SMOKE-001-real-advisory-path-b-blocks-real-core-deletion | real (the toolkit's real enforce-extensions CI job + its real binding.lock + vendored extension nodes) | Path B is found non-blocking (the enforce-extensions convention verdict is advisory / continue-on-error) and the succession guard refuses deleting a real bound-and-mirrored core rule, naming it — the latent enforcement hole exists live | enforce.registry.path_b_is_blocking + guard_core_deletion over the real substrate | #1427 (core-succession-guard; deleting a core node silently strips enforcement) |
| acc:verify-enforcement:M001-SMOKE-001-real-substrate-enforces-zero-extension-rules | real (the toolkit's real .atdd extension nodes + binding.lock + the real .github CI workflow that decides Path B blocking) | the live coverage report finds Path B non-blocking, so the ENFORCED-in-CI set is empty while the bound set is non-empty — every bound extension rule is REPORTED-only, enforced solely by its blocking core twin | enforce.coverage_report.live_coverage_report -> path_b_is_blocking + real declared/bound sets | #1429 (enforcement-coverage-report; the bound set is not the enforced set) |
| acc:verify-enforcement:E001-SMOKE-001-real-bound-twin-is-not-retirement-safe-under-advisory-path-b | real (the toolkit's real .atdd extension nodes + binding.lock + the real .github CI workflow that decides Path B blocking) | the live succession-coverage proof finds Path B non-blocking, so at least one real core rule whose extension twin IS bound is still marked NOT retirement-safe, and the retirement precondition fails for it | enforce.succession.live_succession_coverage + retirement_precondition_holds over the real substrate | #1429 (succession-coverage-proof; the precondition the govern-registry succession guard consumes) |
| acc:author-atdd-substrate:C008-SMOKE-001-no-orphan-nodes | real (live repo corpus: every *.convention.yaml + the core relationship graph, through the strict disposition gate) | every convention node is referenced in the graph as a source_ref/target_ref (0 orphans); the existing forward check (no dangling edges) still passes | *.convention.yaml scan + relationships.yaml -> orphan_nodes -> disposition gate | #1148 (bidirectional convention-node graph integrity) |
| acc:define-plans:D001-SMOKE-001-help-lists-flags | real (`atdd plan --help` subprocess against the CLI shell #758) | help lists --text/--brief-out/--json/--quiet and exposes no subcommand; exit 0 | shell -> atdd plan --help | #760 (lift the define-plans SMOKE suppression; shell-level, full session is #1139) |
| acc:define-plans:E001-SMOKE-001-text-flag-live | real (`atdd plan --text ... --json` subprocess) | brief JSON sources[0] == {type: text, value: 'hello world'}; exit 0 | shell -> atdd plan --text -> source classifier | #760 (define-plans SMOKE backfill) |
| acc:define-plans:E002-SMOKE-001-md-file-live | real (`atdd plan <file>.md --json` subprocess) | brief JSON sources[0].type == 'file'; exit 0 | shell -> atdd plan <path> -> source classifier | #760 (define-plans SMOKE backfill) |
| acc:define-plans:E003-SMOKE-001-pdf-richdoc-live | real (`atdd plan <file>.pdf --json` subprocess) | brief JSON sources[0].type == 'rich_doc'; exit 0 | shell -> atdd plan <path> -> source classifier | #760 (define-plans SMOKE backfill) |
| acc:define-plans:E004-SMOKE-001-dir-codebase-live | real (`atdd plan <dir> --json` subprocess) | brief JSON sources[0].type == 'codebase'; exit 0 | shell -> atdd plan <dir> -> source classifier | #760 (define-plans SMOKE backfill) |
| acc:define-plans:E005-SMOKE-001-no-args-exit2-live | real (`atdd plan` no-source subprocess) | process exits 2; no brief rendered | shell -> atdd plan (no source) -> argparse/exit | #760 (define-plans SMOKE backfill) |
| acc:define-plans:C001-SMOKE-001-json-keys-live | real (`atdd plan --text x --json` subprocess) | stdout/err carries valid JSON with keys sources + brief_out; exit 0 | shell -> atdd plan --json -> brief renderer | #760 (define-plans SMOKE backfill) |
| acc:define-plans:C002-SMOKE-001-clean-import-live | real (`atdd plan --text x` subprocess) | CLI runs end-to-end (exit 0), proving the plan module imports with no atdd.coach/git runtime dependency | shell -> atdd plan -> module import graph | #760 (define-plans SMOKE backfill; runtime proxy for the static no-git/coach-import AST check) |
| acc:define-plans:Y002-SMOKE-001-real-session-runs-start-to-author | real (installed `atdd plan` CLI driving a session in a throwaway checkout; no mocks) | a session runs start -> intent -> attach -> compose -> ratify -> author under the new stage names only; every step exits 0, the session reaches authored with kept units written, and no output names a retired stage | shell -> atdd plan <op> -> PlanSession -> atdd author writers | #1688 (rename the plan-session stages to Intent/Attach/Compose/Ratify) |
| acc:define-plans:Y002-SMOKE-002-help-surfaces-name-the-new-stages | real (installed `atdd --help` and `atdd plan <op> --help` subprocesses) | the top-level gloss reads Intent→Attach→Compose→Ratify→author; main-job/source/unit are glossed Intent/Attach/Compose; reopen says it returns to Compose; README documents the same stage names and --step values | shell -> atdd plan --help -> argparse help strings | #1688 (a unit test over the enum would pass while the CLI still printed the old words) |
| acc:author-atdd-substrate:C009-SMOKE-001-validate-refuses-orphan | real (atdd validate package over a built extension package: valid manifest + schema-valid owned nodes + relationships.yaml, two variants) | the orphan variant raises CompositionError naming the orphan node; the fully-wired variant validates; no runtime executed | extension dir -> validate_package -> extension_orphan_nodes | #1154 (extends no-orphan-nodes to extension graphs via the composition gate) |
| acc:author-atdd-substrate:E006-SMOKE-001-cli-emits-gate-passing-body | real (installed `atdd author issue` CLI in a real checkout) | the emitted issue body validates against issue.schema.json and passes the schema-driven compliance gate (validate_issue_body / E019) with Graph Context + Mirror present and zero placeholders, no manual patching | shell -> atdd author issue -> create_issue_body -> validate_issue_body | #1223 (planned; schema-driven issue body, child of #1221) |
| acc:author-atdd-substrate:C010-SMOKE-001-cli-rejects-tampered-body | real (installed schema-driven compliance check in a real checkout) | a body with `### Graph Context` removed is rejected with a non-zero exit naming the missing required section sourced from issue.schema.json | shell -> validate_issue_body (schema check) over tampered body | #1223 (planned; schema gate supersedes the E019 string-grep) |
| acc:author-atdd-substrate:C011-SMOKE-001-live-drift-guard-over-real-artifacts | real (the shipped issue.schema.json + real create_issue_body + real coach gate in a checkout, no mocks) | issue.schema.json required-sections == create_issue_body fields == E019 required sections across the real artifacts | checkout -> drift-guard over issue.schema.json + create_issue_body + load_required_sections() | #1223 (planned; tri-directional drift-guard keystone) |
| acc:author-atdd-substrate:K002-SMOKE-001-real-issue-bodies-validate | real (a sample of real existing GitHub issue bodies as checked-in fixtures, validated in a real checkout) | every sampled real compliant body validates against issue.schema.json; none that passed legacy E019 is newly rejected | fixtures (real issue bodies) -> validate against issue.schema.json | #1223 (planned; back-compat / migration guarantee) |
| acc:author-atdd-substrate:E008-SMOKE-001-author-issue-publishes-to-live-store | real (installed `atdd author issue` CLI against a real Control Root + State Store; a stubbed `gh` on PATH returning a canned issue URL, no production GitHub) | a work_item keyed by <slug> exists (state INIT, data carrying title/type/branch/train/feature/body) and exactly one github issue external_ref is linked to it (one-per-issue) — the command created, not merely authored | shell -> atdd author issue -> atdd.state.work_item_writer.create_work_item (store-first) + integrations.github create -> external_refs.link | #1272 (planned; author publishes store-first by default) |
| acc:author-atdd-substrate:C012-SMOKE-001-store-unreachable-fails-loud | real (installed `atdd author issue` CLI in a real checkout with the State Store made unreachable, e.g. ATDD_CONTROL_ROOT at an unwritable location) | the CLI exits non-zero, emits no schema-valid body to stdout (no body-only degrade), and names the store as the unreachable dependency — the #1271 orphan cannot recur | shell -> atdd author issue (generate path) -> store-first publish fails loud | #1272 (planned; author-without-publish recurrence guard) |
| acc:author-atdd-substrate:C014-SMOKE-001-complete-gate-answers-from-the-bound-rule | real (a throwaway git checkout driven into the post-merge state auto-phase sees — on the default branch at the commit the PR landed — against the real IssueManager and the real convention registry; no mocks of the policy) | the COMPLETE runtime gate answers the artifacts question from the bound convention rule: an empty declaration yields the declared violation instead of the `total == 0` free pass, and an accurate declaration yields none while each claim is still reported against git | real git checkout -> IssueManager artifact verification -> shared bound checker -> bind_rule/registry disposition | #1726 (planned; the Artifacts claim was enforced twice and bound to nothing) |
| acc:govern-lifecycle:E019-SMOKE-001-revise-publishes-to-live-store | real (installed `atdd author issue --revise` CLI against a real Control Root + State Store; a real `gh` subprocess resolved from a stubbed binary on PATH, selectable to exit 0 or non-zero, no production GitHub) | the live store carries the revised body/type with the lifecycle state unchanged and an issue_revised event recorded; a reachable provider leaves the outbox empty, and an unreachable one still exits 0 with the store revision standing and the body update deferred to the outbox rather than lost | shell -> atdd author issue --revise -> work_item_writer.revise_work_item_issue (store-first, authoritative) -> integrations.github update_body (projection) -> sync.enqueue_outbox on failure | #1477 (the mint-path decommission retired E019's two mint-path SMOKE acceptances; SMOKE coverage restored against the live author-revise subject the WMBT was re-pointed at) |
| acc:govern-lifecycle:E056-SMOKE-001-real-gate-scopes-to-current-pr | real (the real coach.pr.merge-blocks-on-pre-smoke-close validator module + a real env where GITHUB_REF/PR_NUMBER name a clean PR while an offending PR exists in the scan) | the gate resolves the current PR from the environment and feeds the disposition gate only that PR's violations (clean → passes); the offending PR's violation is still produced and logged | _current_pr_number(env) -> scope blocking to current PR -> assert_disposition_satisfied | #1165 (per-PR scoping; offender still blocked on its own CI) |
| acc:govern-lifecycle:R005-SMOKE-001-real-validate-coach-enforces-projection-only | real (a real `pytest` subprocess running the real validator over a real copy of the repo's `.github/workflows/`; no in-process import shortcut, no synthetic fixture tree) | CLEAN run passes AND reports the sweep as PASSED (a skipped guard is green too); PLANTED run FAILS naming `coach.phase-label.projection-only` and the offending `post-merge-lifecycle.yml:<line>`, through the real strict disposition gate | workflow file → projection-only scanner → Violation → strict disposition gate → pytest exit code | #1452 (the raw `gh issue edit --add-label atdd:COMPLETE` writer; both halves of the swap replanted verbatim and proven red. The writer already survived #1434, whose stated purpose was to remove it — #1434 was itself stamped atdd:COMPLETE with its own store at SMOKE) |
| acc:govern-lifecycle:R006-SMOKE-001-real-reconcile-state-cli | real (the installed CLI as a real subprocess against a real Control Root + real migrated State Store carrying real work items; only the network boundary stubbed — a real `gh` executable on PATH answering `issue list`/`pr list`, the same seam R005/E019 use) | the verb is proven to actually dispatch before any output is asserted; the SMOKE-floored record reports as class 2 naming `REFACTOR -> COMPLETE`; a default report leaves objects.state at SMOKE and says DRY RUN; the INIT-floored record exits non-zero naming REFUSED with its store still INIT; `--all --apply` is refused as report-only | shell → atdd coach reconcile-state → coach_verbs dispatch → build_report → WorkItemReader (real store) → classify → apply_repair refusal → exit code | #1338 (the 236 drifted records; 82 legacy-undriven refused rather than replayed — operator-confirmed. Fabricated history is unreachable by construction: no flag unlocks a replay) |
| acc:govern-lifecycle:C010-SMOKE-001-real-token-does-not-replay-across-checkouts | real (a real Python subprocess outside the source tree exercising the installed `atdd.coach.gate.approval` signing path; no in-process import of the module under test, no monkeypatching, no fake token) | a token minted for `feat/alpha` verifies True on `feat/alpha` and False on `feat/beta`, so the guard discriminates rather than refusing everything — the replay-across-branch property is closed in the shipped artifact | subprocess -> build_token(branch, expires_at) -> canonical_scope binds branch+expiry -> verify_token(branch=...) -> JSON verdicts asserted | #1525 (19 of 19 main-signed tokens verified from foreign worktrees, measured 2026-07-18. Refusal at the TRANSITION gate additionally needs the gate consumer to pass the current branch; that path seam is owned by #1376 and untouched here) |
| acc:govern-lifecycle:C012-SMOKE-001-real-mint-records-the-acting-session | real (the real `atdd coach approve` CLI in a real subprocess under a pinned agent-session env, read back by the real `ApprovalTokenGateCheck` in a second real process; no in-process import of the mint or the check, no monkeypatching, token written under a temp root so the live corpus is untouched) | the minted token records the observed provider + session id and does NOT record the shell account as its approver, the real gate reports it as session-attributed, and the same real gate reports a pre-fix (no `schema_version`) token as UNATTRIBUTED | subprocess `python -m atdd.cli coach approve` -> resolve_session() -> build_token(agent_session) -> canonical_scope binds the actor -> second subprocess ApprovalTokenGateCheck -> describe_attribution | #1718 (169 live tokens measured 2026-08-03, 162 naming a human and an unknown number agent-minted; `approved_by` sat outside the signature and a rewrite still verified. Branch/expiry binding at these same call sites is #1376's seam and is untouched here) |
| acc:govern-lifecycle:C020-SMOKE-001-the-real-command-refuses-the-edge-the-issue-is-not-on | real (the real `atdd coach approve` CLI in a separate real process against a real migrated State Store under a temp Control Root; no in-process import of the command, no monkeypatching, the source tree first on PYTHONPATH so the installed wheel cannot answer instead) | the shipped command refuses `PLANNED->RED` for an issue standing at INIT, names INIT in its output and leaves no token file behind, and the SAME command mints once the issue genuinely reaches PLANNED — so the refusal discriminates rather than blocking the command outright | subprocess `python -m atdd.cli coach approve` -> _parse_transition (phase-machine vocabulary + edge legality) -> resolve_issue_phase(external_refs -> work_item.state) -> refuse before mkdir/write, then the same path admitting the live edge | #1735 (two tokens minted for PLANNED->RED and SMOKE->REFACTOR while #1726 sat at INIT with all five transitions freshly refused, measured 2026-08-03; every orphan token was written by the REAL command, which is why the shipped artifact is the subject here) |
| acc:author-plan-substrate:E006-SMOKE-001-seed | real (bind_rule against the live train.convention.yaml rules: block + the bound dispatch check) | the 3 executable-train rules resolve strict + the bound check catches a real composite-key-without-behavioral_difference violation | train.convention.yaml rules: -> bind_rule -> disposition gate | #1054 (Phase 1 executable-train binding); #1083 (executable-train rules) |
| acc:author-plan-substrate:C008-SMOKE-001-seed | real (live `atdd plan unit --spec` CLI) | a spec that is valid JSON but not an object, and an unparseable spec, are both refused with exit 2 naming --spec, no unit persisted; `--spec @<path>` loads an object from a file | atdd plan unit --spec -> _parse_spec (@file / inline JSON) -> validate_author_spec -> AuthorInputError -> exit 2 | #1403 (executing: test_c008_smoke_001_plan_unit_spec_cli.py drives the real CLI via subprocess, run-or-fail) |
| acc:define-plans:D001-SMOKE-001-seed | real (live atdd plan session CLI) | advancing to Locate before a main-job is set is refused | atdd plan session start -> main-job -> advance --step locate | #1209 (pending live-session smoke; RED retargets session tests) |
| acc:define-plans:L001-SMOKE-001-seed | real (live atdd plan session CLI) | a source captured in Locate is bound to session state and survives reload | atdd plan session source -> session state -> reload | #1209 (pending live-session smoke) |
| acc:define-plans:P001-SMOKE-001-seed | real (live atdd plan session CLI) | a Prepare unit without a valid atdd author spec is rejected | atdd plan session unit --kind --ref --spec -> author-input validation | #1209 (pending live-session smoke) |
| acc:define-plans:C001-SMOKE-001-seed | real (live atdd plan session CLI) | no artifact is written before the operator confirms | atdd plan session author -> confirm gate -> atdd author | #1209 (pending live-session smoke; mirrors confirm-before-author) |
| acc:define-plans:C002-SMOKE-001-seed | real (live atdd plan session CLI) | a step advance is refused unless its exit condition holds | atdd plan session advance --step -> gate check | #1209 (pending live-session smoke) |
| acc:define-plans:E001-SMOKE-001-seed | real (live atdd plan session CLI) | Confirm invokes atdd author once per locked unit | atdd plan session confirm -> author per unit | #1209 (pending live-session smoke) |
| acc:define-plans:Y001-SMOKE-001-seed | real (live atdd plan session CLI) | keep/pivot/kill is recorded via the elicit channel | atdd plan session decide --verdict -> elicit | #1209 (pending live-session smoke) |
| acc:define-plans:C003-SMOKE-001-wagon-verb-object-blocks-at-confirm | real (PlanSession.confirm + atdd validate planner) | confirm refuses to lock a kept wagon unit named `mode-select`; a verb-object slug `manage-users` locks | atdd plan session confirm -> assert_kept_wagon_feature_naming -> SessionGateError | #1276 (foundational planner naming enforcement) |
| acc:define-plans:C003-SMOKE-002-cli-exit-nonzero-on-naming-refusal | real (plan_session_cli.run end-to-end) | `atdd plan confirm` returns non-zero (2) on a verb-object refusal and 0 on a verb-object name; guarded against regression of the shared gate-refusal path | shell -> atdd plan confirm -> SessionGateError -> return 2 | #1276 (exit-code fold-in; pins the shared refusal path) |
| acc:define-plans:C004-SMOKE-001-feature-verb-object-blocks-at-confirm | real (PlanSession.confirm + atdd validate planner) | confirm refuses to lock a kept feature unit `feature:manage-users:respond-and-preview`; `feature:manage-users:make-choice` locks | atdd plan session confirm -> assert_kept_wagon_feature_naming -> SessionGateError | #1276 (foundational planner naming enforcement) |
| acc:define-plans:E002-SMOKE-001-naming-rules-bound-and-run | real (`atdd validate planner --local` + bind_rule) | test_wagon_name_is_verb_object + test_feature_name_is_verb_object are collected and bind_rule resolves both rule_ids | shell -> atdd validate planner -> pytest collect + bind_rule | #1276 (foundational planner naming enforcement) |
| acc:define-plans:C005-SMOKE-001-theme-first-blocks-at-confirm | real (PlanSession.confirm + atdd validate planner) | confirm refuses to lock a kept wagon producing `round:result`; a theme-first identity `commons:identifiers.uuid` locks | atdd plan session confirm -> assert_kept_artifact_naming -> SessionGateError | #1329 (foundational artifact/contract naming enforcement) |
| acc:define-plans:C005-SMOKE-002-cli-exit-nonzero-on-artifact-naming-refusal | real (plan_session_cli.run end-to-end) | `atdd plan confirm` returns non-zero on a non-theme-first produced artifact and 0 on a theme-first one; pins the shared gate-refusal path | shell -> atdd plan confirm -> SessionGateError -> non-zero exit | #1329 (exit-code fold-in; pins the shared refusal path) |
| acc:define-plans:C006-SMOKE-001-path-mirror-blocks-at-confirm | real (PlanSession.confirm + atdd validate planner) | confirm refuses to lock a produced `commons:identifiers.uuid` whose contract is `contracts/commons/WRONG/uuid.schema.json`; the mirrored path locks | atdd plan session confirm -> assert_kept_artifact_naming -> SessionGateError | #1329 (foundational artifact/contract naming enforcement) |
| acc:define-plans:E003-SMOKE-001-artifact-naming-rules-bound-and-run | real (`atdd validate planner --local` + bind_rule) | test_artifact_theme_first_identity + test_artifact_path_mirrors_identity are collected and bind_rule resolves both rule_ids | shell -> atdd validate planner -> pytest collect + bind_rule | #1329 (foundational artifact/contract naming enforcement) |
| acc:validate-conventions:E001-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1204 RED) | registry.yaml declares exactly the 13 canonical families and 22 templates | atdd plan author -> conventions registry -> conformance smoke | #1204 (pending RED) |
| acc:validate-conventions:E002-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1204 RED) | each family archetype.py exposes exactly the template ids registry.yaml assigns it | conventions registry <-> archetype exports roundtrip | #1204 (pending RED) |
| acc:validate-conventions:E003-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1204 RED) | template_contract.py defines all eight mandatory template metadata fields | _support/template_contract.py introspection | #1204 (pending RED) |
| acc:validate-conventions:E004-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1204 RED) | every family folder contains README.md, archetype.py and fixtures.py | conventions/<family>/ filesystem presence | #1204 (pending RED) |
| acc:validate-conventions:E005-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1204 RED) | legacy persona validator folders are unmoved and atdd validate behaviour is unchanged | legacy validators run unchanged in parallel | #1204 (pending RED) |
| acc:validate-conventions:E010-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1206 RED) | every P0 direct/split/merged/superseded entry has an impl or superseded evidence | parity map P0 -> conventions/<family>/test_<variant>.py | #1206 (pending RED) |
| acc:validate-conventions:E011-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1206 RED) | every variant references a registry template and declares the nine metadata fields | variant module metadata introspection | #1206 (pending RED) |
| acc:validate-conventions:E013-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1206 RED) | no target validator imports a legacy persona validator module | import-graph scan of conventions/ | #1206 (pending RED) |
| acc:validate-conventions:E014-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1206 RED) | target and legacy validators run in parallel without collision | dual-suite pytest run | #1206 (pending RED) |
| acc:validate-conventions:E015-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1207 RED) | CI rejects a new convention-graph validator under a persona folder | persona-folder placement gate | #1207 (pending RED) |
| acc:validate-conventions:E018-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1207 RED) | after promotion convention validators run as the authoritative path | validation entrypoint routing | #1207 (pending RED) |
| acc:validate-conventions:Y003-SMOKE-001-no-dangling-legacy-reference | real (live subprocess pytest over the composed convention graph) | no rule implementation.ref/validator resolves to a deleted src/atdd/*/validators/test_*.py (complements Y001 map-safety at the rule-ref level) | composed graph -> per-rule ref resolution -> gate | #1365 (live verified 2026-07-08; PASSED after deleting 9 legacy validators, quarantining 12) |
| acc:validate-conventions:Y003-SMOKE-002-coverage-preserved | real (live subprocess pytest, YAML-scan of all convention rule bindings) | every repointed rule binds to an executing convention variant (Phase GREEN, real fault/parity test), not an xfail/stub (complements Y002 preflight visibility) | repointed rule -> convention variant nodeid -> executes on real graph | #1365 (live verified 2026-07-08; PASSED — 19 rules repointed to executing variants) |
| acc:validate-conventions:E019-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1211 RED) | graph exposes id/kind/refs/theme/package/location on real nodes | convention validator -> real graph -> gate | #1211 (pending RED) |
| acc:validate-conventions:E020-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1211 RED) | every variant selects >0 real nodes or declares allowed-empty | convention validator -> real graph -> gate | #1211 (pending RED) |
| acc:validate-conventions:E021-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1211 RED) | variant evidence keys subset of template failure_evidence | convention validator -> real graph -> gate | #1211 (pending RED) |
| acc:validate-conventions:E025-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1211 RED) | train nodes loaded from detail files conform to train schema | convention validator -> real graph -> gate | #1211 (pending RED) |
| acc:validate-conventions:E026-SMOKE-001-seed | seed (PLANNED stub; concrete SMOKE authored in #1211 RED) | acceptance harness enum includes smoke | convention validator -> real graph -> gate | #1211 (pending RED) |
| acc:validate-conventions:E032-SMOKE-001-session-graph-in-real-checkout | real (subprocess `python -m pytest` over a real checkout; no synthetic fixture) | the composed convention graph is built once per pytest session and reused by read-only tests; suite still collects >= 218 tests and Y003 stays green | session fixture -> load_composed_graph -> baseline tests | #1414 (live verified — read-only suite composes the graph exactly once; 35 -> 1 builds) |
| acc:validate-conventions:E033-SMOKE-001-no-yaml-written-in-real-checkout | real (subprocess `python -m pytest` over the binding family in a real checkout; no synthetic fixture) | binding fault suite exits 0, git status reports no modified convention YAML, and the fault is still caught (coverage preserved) | in-memory clone_graph -> rename_rule_id -> roundtrip template | #1415 (live verified — binding faults inject into a cloned graph; 13 -> 1 builds, zero YAML writes) |
| acc:validate-conventions:E034-SMOKE-001-no-yaml-written-in-real-checkout | real (subprocess `python -m pytest` over the five migrated evaluator families — coherence/presence/resolution/coverage/acyclicity — in a real checkout; no synthetic fixture) | the migrated fault suite exits 0, git status reports no residual modified convention or plan YAML, and the migrated faults are still caught (coverage preserved) | in-memory clone_graph -> add_node/set_node_field/remove_node_field/break_ref/replace_field_value -> family template | #1416 (live verified — evaluator faults inject into a cloned graph; 72 -> 53 builds, zero YAML writes) |
| acc:validate-conventions:E035-SMOKE-001-no-file-written-in-real-checkout | real (subprocess `python -m pytest` over the last seven fault families — policy/schema/grammar/composition/boundary/sizing/uniqueness — in a real checkout; no synthetic fixture) | the migrated fault suite exits 0, `git status` reports no modification to ANY tracked file (not just YAML — these families wrote git hooks, pyproject.toml and a committed SMOKE test), the clean graph is composed exactly once across all seven, and the faults are still caught (coverage preserved) | staged tmp tree via mirror_file/stage_file -> graph_rooted_at -> family template; node faults via clone_graph -> node_at/set_node_field | #1458 (live verified — root-reader faults inject into a staged root; 53 -> 22 builds, zero writes to the checkout) |
| acc:validate-conventions:E036-SMOKE-001-split-collects-the-whole-suite | real (subprocess `python -m pytest --collect-only` over the real conventions suite, un-split and under each -m selector; no synthetic fixture) | parallel + serial collected counts sum to the un-split total and neither half is empty — no test falls through both marker filters and silently stops running | pyproject markers -> conftest default class -> pytest -m selection | #1418 (live verified — 246 parallel + 3 serial = 249 collected. The 9 remaining tree-writers were staged onto temp roots: their evaluators read through `graph.root`, never the checkout's identity, so a mirrored surface plus a re-rooted graph runs the identical code path. E034's 2 smokes then CASCADED — they never wrote anything themselves, only spawned a pytest over families that did. The 3 that stay serial are irreducible BY DESIGN: they exist to prove the retired on-disk mechanism still writes, so they must observe the real tree) |
| acc:validate-conventions:E036-SMOKE-002-ci-runs-both-subsets-and-the-gate-fans-in-both | real (the checked-in .github/workflows/atdd-validate.yml, parsed) | validate-conventions selects the non-mutating half, installs pytest-xdist and runs -n auto; validate-conventions-serial selects the mutating half and never runs under -n; validate-gate needs both jobs and reads both results | workflow jobs -> validate-gate needs + result check | #1418 (live verified — a red half cannot pass the gate unread) |
| acc:enforce-binding-plan:E001-SMOKE-001-conformance | planned (PLANNED; concrete SMOKE authored, RED pending) | all 26 bound rules run end-to-end, 0/26 emit 'report test missing' | binding.lock -> provider CLI -> raw v1.1 records | #1238 (V1; pending RED) |
| acc:enforce-binding-plan:E002-SMOKE-001-exit-code | planned (PLANNED; concrete SMOKE authored, RED pending) | clean tree exits 0, one print violation exits 1 at file:line:col, malformed config exits 2 | atdd enforce -> verdict bridge -> exit code | #1238 (V2; pending RED) |
| acc:enforce-binding-plan:E003-SMOKE-001-run-on-commit | planned (PLANNED; concrete SMOKE authored, RED pending) | clean commit passes, dirty print commit blocked locally + red in CI, no-op when no bound rules, honors CI=true | commit hook + CI job -> binding-plan scan -> gate | #1238 (V3; pending RED) |
| acc:enforce-binding-plan:E004-SMOKE-001-dogfood-parity | planned (PLANNED; concrete SMOKE authored, RED pending) | runner verdicts over src/atdd match legacy validators, print exemption of src/atdd preserved | runner -> src/atdd scan -> diff vs legacy validators | #1238 (V4; the evidence #1207 needs; pending RED) |
| acc:enforce-binding-plan:E005-SMOKE-001-boundary | planned (PLANNED; concrete SMOKE authored, RED pending) | no atdd.workspace.* import in core verb, no atdd.* core import in provider CLI | import-discipline guard -> core + workspace CLI | #1238 (V5; pending RED) |
| acc:enforce-binding-plan:E006-SMOKE-001-rematerialization | planned (PLANNED; concrete SMOKE authored, RED pending) | chosen vendor-vs-lock path has a passing automated check (rebuild-from-lock or digest-matched copies) | substrate.lock + vendored trees -> re-materialization check | #1238 (V6; D-5 default tracked-copies+digest; pending RED) |
| acc:coach-verb-split:E001-SMOKE-001-real-transition-in-temp-control-root | planned (PLANNED; concrete SMOKE authored at RED) | throwaway slug under a temp ATDD_CONTROL_ROOT drives a real State Store + manifest phase write; deprecated `atdd issue --status` reaches the same engine; NEVER a live GitHub issue | atdd coach transition -> issue_transition.apply_transition -> State Store + manifest mirror (temp control root) | #1304 (pending RED) |
| acc:coach-verb-split:E002-SMOKE-001-real-reconcile-in-temp-control-root | planned (PLANNED; concrete SMOKE authored at RED) | under a temp ATDD_CONTROL_ROOT / temp cwd with no manifest, `atdd coach reconcile` reaches the real IssueManager.reconcile() (proven by its manifest-not-found guard, no UnboundLocalError, no live gh call); deprecated `atdd issue reconcile` reaches the same engine + warns on stderr; NEVER a live GitHub issue | atdd coach reconcile -> coach_verbs/reconcile.py -> IssueManager.reconcile() (temp control root, bails at manifest guard) | #1305 (pending RED) |
| acc:coach-verb-split:E003-SMOKE-001-real-read-in-temp-control-root | real (executes: test_e003_smoke_001_coach_issues_read_parity.py, 1 passed after C5b (#1309) retired the deprecated-shim arm; real subprocess, NO collaborator substitution per tester.smoke.no-collaborator-substitution/#1298) | `atdd coach issues open` runs as a REAL subprocess against the real repo — real CLI dispatch → coach_verbs resolver → IssueManager.open_issues → real GitHub boundary. The deprecated `atdd issue open` arm was RETIRED by C5b (#1309) together with the surface it tested. READ-ONLY (list mutates nothing, no worktree) so NEVER touches/archives a live issue (show/enter parity lives in the hermetic E003 INTEGRATION suite) | python -m atdd coach issues open (subprocess) -> coach_verbs/issues.py -> issue_read.run -> IssueManager.open_issues -> real gh boundary | #1307 (SMOKE, executes) |
| acc:coach-verb-split:E004-SMOKE-001-real-sync-labels-in-temp-control-root | real (a REAL `python -m atdd` child subprocess under a temp ATDD_CONTROL_ROOT; only the `gh` transport is faked on PATH — no self-skip, no env gate, never a live GitHub issue) | the real verb+derivation decides the add-delta, recorded via the fake `gh issue edit --add-label` == the body-derived expected set {atdd:INIT, archetype:coach, wagon:govern-lifecycle}; the deprecated `atdd issue sync-labels` arm was RETIRED by C5b (#1309) with the surface | python -m atdd coach sync-labels -> coach.run_cli -> resolve_verb -> sync_labels.run -> IssueManager.sync_labels -> _derive_expected_labels -> GitHubClient (fake gh) | #1308 (live-verified 2026-07-08 at SMOKE; recorded add == body-derived set; honors #1298 real-execution) |
| acc:coach-verb-split:E005-SMOKE-001-real-remaining-verbs-and-hook-gate-in-temp-control-root | real (executes: test_e005_remaining_verbs_extraction.py::TestRemainingVerbsSmokeInTempControlRoot, 2 passed after C5b (#1309) retired the deprecated-shim arm; REAL `python -m atdd` child subprocesses under a temp ATDD_CONTROL_ROOT / temp cwd — no self-skip, no collaborator substitution per #1298, never a live GitHub issue) | `atdd coach is-registered <unregistered-branch>` in an isolated empty temp cwd drives the REAL IssueManager.branch_is_registered gate (returns True "nothing to check" → exit 0, no UnboundLocalError/traceback, no live gh mutation — proving the verb delegated to the real gate); the deprecated `atdd issue is-registered` arm was RETIRED by C5b (#1309) with the surface; the pre-commit hook line invokes `atdd coach is-registered "$BRANCH"` so the gate survives C5b's monolith deletion | python -m atdd coach is-registered -> coach.run_cli -> resolve_verb -> is_registered.run -> IssueManager.branch_is_registered (temp control root) | #1382 (C5a; live-verified 2026-07-08 at SMOKE — 3 passed; real subprocess exit 0 + stderr deprecation observed; honors #1298 real-execution) |
| acc:drive-state-machine:E003-SMOKE-001-live-consolidation-yields-single-store | real (a genuine `git worktree` flat-sibling layout in tmp + real State Store sqlite + real `atdd state` CLI; no stubs, git-skipped only when git is absent) | after `migrate-layout`: exactly one control-root store on disk, per-worktree DBs deleted, one deduped work_item per GitHub issue, `layout --check` exits 0 | seed per-worktree stores -> `atdd state migrate-layout` -> merge/dedup-by-external_refs/delete -> `atdd state layout --check` | #1346 (completes #1315; live full-project consolidation is operator-coordinated post-merge+install — running it before the toolkit ships Rule 1.5 would break the installed CLI on every worktree) |
| acc:drive-state-machine:E004-SMOKE-001-write-from-worktree-with-env-lands-in-shared-store | real (a genuine `git worktree` flat-sibling layout in tmp; the interim ATDD_CONTROL_ROOT=<worktree> workaround set; real resolver + sqlite) | a hot-path store open from the worktree lands in the shared control-root store; no rogue per-worktree state.sqlite is created | `ATDD_CONTROL_ROOT=<wt>` -> `init_state_store(start=wt)` -> resolver Rule 1.4 redirect -> shared store | #1346 (write-hot-path activation) |
| acc:drive-state-machine:M002-SMOKE-001-doctor-flags-and-clears-rogue-store | real (a genuine `git worktree` flat-sibling layout in tmp + real `atdd state` CLI) | `layout --check` exits non-zero and names a rogue per-worktree store, exits 0 once it is removed | seed control-root store -> add rogue worktree store -> `atdd state layout --check` (before/after removal) | #1346 (#1220 single-store guard bites) |
| acc:drive-state-machine:E005-SMOKE-001-live-extension-install-lands-in-control-root | real (a genuine `git worktree` flat-sibling layout in tmp + real control-root resolver + real substrate installer `install_path`) | the substrate install target resolves to `<project>/.atdd/extensions/<id>/<ver>`, and no `<wt>/.atdd/extensions/` is created | `ATDD_CONTROL_ROOT`-free worktree -> `resolve_operational_root(wt)` -> `installer.install_path(control_root, ...)` | #1346 (extension-install activation) |
| acc:drive-state-machine:E006-SMOKE-001-live-consolidation-yields-single-operational-atdd | real (a genuine `git worktree` flat-sibling layout in tmp + real sqlite store + real `atdd state` CLI) | after `migrate-layout`: control-root `.atdd/` holds the merged store AND folded extensions, per-worktree store+extensions gone, `layout --check` exits 0 | seed per-worktree store + extensions -> `atdd state migrate-layout` -> fold+delete -> `atdd state layout --check` | #1346 (generalized consolidation: store + extensions) |
| acc:drive-state-machine:M003-SMOKE-001-layout-check-flags-rogue-extensions-live | real (a genuine `git worktree` flat-sibling layout in tmp + real `atdd state` CLI) | `layout --check` exits non-zero naming a rogue per-worktree `.atdd/extensions/` install, exits 0 once removed | seed control-root store -> add rogue worktree extensions -> `atdd state layout --check` (before/after removal) | #1346 (generalized #1220 guard: operational .atdd/ subtree) |
| acc:coach-verb-split:E006-SMOKE-001-real-subprocess-removal-and-repointed-blockers | real (executes: test_e006_smoke_001_real_removal_and_repointed_blockers.py, 8 passed; REAL `python -m atdd` child subprocesses under a temp ATDD_CONTROL_ROOT, with the repo's `src/` prepended to the child PYTHONPATH so the removal is proven against the CODE UNDER TEST and not the pipx-installed toolkit — the exact #1298 self-deception this row exists to rule out; no self-skip, no collaborator substitution, never a live GitHub issue) | `atdd issue open` exits NON-ZERO from a real process and its stderr names `atdd coach` + `atdd author issue` with no argparse `invalid choice` (the pre-parse removal guard); `atdd --help` no longer lists an `issue` subcommand; `atdd author issue --dry-run` exits 0, prints a rendered body, and creates NO State Store under the temp control root (the ported capability writes nothing); the shipped gh.shim + pre-commit `gh issue create` blockers name `atdd author issue` and never emit `atdd issue <slug>`; issue.convention.yaml `cli_commands:` and the CONDUCTOR.md managed `issues:` block present only coach/author forms; no `_deprecation_warning` CALLSITE (not the deduped registry) recommends `atdd issue` | python -m atdd issue open (subprocess, src on PYTHONPATH) -> cli.main -> _removed_command_guard -> rc=2 + replacement map on stderr; python -m atdd author issue --dry-run -> author.run -> create_issue_body -> validate_issue_body -> stdout (returns BEFORE the publish import: no store, no gh) | #1309 (C5b, closes umbrella #1303; live-verified 2026-07-09 at SMOKE — 8 passed; honors #1298 real-execution) |
| acc:reconcile-dispositions:D001-SMOKE-001-model-imports-cleanly-in-the-installed-package | real (imports `atdd.enforce.dispositions` from the installed package; no mocks) | the treatment vocabulary, the wiring value `bound`, and the gate name are all reachable as named module attributes without an import error | `import atdd.enforce.dispositions` -> read TREATMENT_DISPOSITIONS + BOUND_DISPOSITION + DISPOSITION_GATE | #1424 (D001; disposition namespace model) |
| acc:reconcile-dispositions:E001-SMOKE-001-enforce-does-not-fail-a-bound-documentation-only-rule | real (runs the enforce verdict over the committed `.atdd/binding.lock.yaml` + vendored substrate; no mocks) | the verdict for the bound documentation-only rule `tester.filename.urn` is not `fail` (documentation-only never fails the build) | `atdd enforce` -> runner.enforce -> `_verdict_for_rule(tester.filename.urn, raw)` -> not fail | #1424 (E001; total disposition-to-verdict mapping; fixes the live documentation-only false failure) |
| acc:reconcile-dispositions:E002-SMOKE-001-every-vendored-node-declares-an-in-vocabulary-treatment | real (reads every bound convention node under the committed `.atdd/extensions` tree; no mocks) | reading the disposition of every bound vendored node raises no vocabulary error — all declare in-vocabulary treatments | `.atdd/binding.lock.yaml` bound entries -> `rule_metadata(substrate_home, rule_id)` for each -> no UnknownDispositionError | #1424 (E002; disposition vocabulary validation) |
| acc:bind-extension-conventions:L001-SMOKE-001-live-inventory-is-coherent-after-fanout | real (reads the committed `.atdd/extensions` nodes + regenerated `.atdd/binding.lock.yaml`; no mocks) | the live binding-gap inventory reports no unbound gating obligation and only the two documentation-only nodes (`coder.performance.perf`, `tester.migration.naming`) as declared-not-bound | `.atdd/extensions/*.convention.yaml` + `binding.lock.yaml` -> `compute_binding_gap` -> gating_unbound == [] + doc_only_unbound partition | #1426 (L001; binding-gap inventory over the real substrate) |
| acc:bind-extension-conventions:E001-SMOKE-001-live-fanout-binds-all-gating-rules | real (composes the binding plan from the real `.atdd/workspaces` manifests + enabled substrate; no mocks) | every one of the 26 fanned-out gating conventions appears as a `bound` entry and the plan binds 52 conventions with no new implementation | `atdd substrate bind` -> `build_binding_plan` -> `index_by_convention` (list realizes_convention) -> 52 bound entries | #1426 (E001; implementation fan-out binding; absorbs #1359 fan-out) |
| acc:bind-extension-conventions:E002-SMOKE-001-live-gating-coverage-is-clean-after-fanout | real (runs the gating-coverage validator over the committed nodes + regenerated lock; no mocks) | the validator returns clean — no unbound gating extension node and no bound entry whose obligation is declared nowhere in the core-or-extension node universe | `.atdd/extensions` + core `conventions/nodes` + `binding.lock.yaml` -> `assert_gating_coverage` -> no violation | #1426 (E002; gating-node binding validation) |
| acc:enforce-conventions-ci:E001-SMOKE-001-real-workflow-enforces-blockingly | real (reads the repository's real committed `.github/workflows/atdd-validate.yml`; no mocks) | the `enforce-extensions` convention-verdict step carries neither `continue-on-error: true` nor `\|\| true` (a strict FAIL exits the job non-zero), it invokes the ratchet baseline so the pre-existing debt is held flat, and the job is a member of the `validate-gate` `needs` fan-in whose result check fails on a non-success | `.github/workflows/atdd-validate.yml` -> `enforce.registry.path_b_is_blocking` + validate-gate `needs`/result-check reader -> blocking + required | #1428 (E001; promotes the #1359 advisory ratchet stage to a REQUIRED blocking check — the point of train 0007) |
| acc:enforce-conventions-ci:E002-SMOKE-001-real-strict-violation-exits-non-zero | real (a real vendored workspace provider CLI subprocessed over a real bound strict convention + real binding lock; no mocks, no stubbed verdict) | the aggregate verdict is FAIL, `EnforceResult.exit_code` is 1, the CLI adapter returns that 1 (so the process exits non-zero), and the printed report names the failing strict rule | real substrate -> `runner.enforce` -> provider subprocess emits a v1.1 violation -> `_verdict_for_rule` = fail -> `EnforceResult.exit_code` = 1 -> `enforce.cli.run` returns 1 | #1428 (E002; PINS the pre-existing exit-code propagation the blocking gate now rests on — a silent false green is the one failure mode a required check cannot tolerate) |
| acc:enforce-conventions-ci:E003-SMOKE-001-real-baseline-neutralizes-the-live-debt | real (loads the repository's real committed `.atdd/enforce-ratchet.yaml` + real `.atdd/binding.lock.yaml`; no mocks) | applying the real baseline to the verdicts it was recorded from yields PASS / exit 0 (the blocking job is green over the live debt), regressing any single baselined rule by one violation flips the same run to FAIL / exit 1 (the ratchet is live, not a blanket exemption), and every rule the baseline registers is a rule the real lock actually binds | `.atdd/enforce-ratchet.yaml` -> `enforce.ratchet.load_baseline` + `apply_ratchet(EnforceResult, baseline)` -> held-flat verdicts / exit 0, +1 violation -> exit 1 | #1428 (E003; the ratchet baseline is the PRECONDITION that makes the E001 blocking flip safe over the 23 live failing rules) |

<!-- train:object-conflict-resolution:project-state (#1400): planned SMOKE acceptances, classified at GREEN -->
| acc:enforce-merge-authority:E002-SMOKE-001-illegal-projection-rejected-end-end | planned — train:object-conflict-resolution:project-state (M3), RED (not yet implemented) | the run exits non-zero | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:govern-projection-fields:E001-SMOKE-001-cli-blocks-wrong-writer-diff | planned — train:object-conflict-resolution:project-state (M4), RED (not yet implemented) | The command exits non-zero | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:isolate-provider-boundary:E002-SMOKE-001-cli-writes-extensions-lock | planned — train:object-conflict-resolution:project-state (M5), RED (not yet implemented) | .atdd/extensions.lock exists and validates against its schema | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:isolate-provider-boundary:K001-SMOKE-001-core-extension-end-to-end | planned — train:object-conflict-resolution:project-state (M5), RED (not yet implemented) | The core workflow completes and CI merge authority passes without consulting the provider | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:migrate-projection-authority:E001-SMOKE-001-cli-migrates-real-manifest | planned — train:object-conflict-resolution:project-state (M8), RED (not yet implemented) | A .atdd/state/projection/ tree is produced with one <uid>.yaml per manifest work item | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:migrate-projection-authority:K001-SMOKE-001-collaborates-through-projection-only | planned — train:object-conflict-resolution:project-state (M8), RED (not yet implemented) | Checkout B observes A's new phase from the committed projection alone | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:project-shared-state:C002-SMOKE-001-ci-job-blocks-noncanonical-push | planned — train:object-conflict-resolution:project-state (M1), RED (not yet implemented) | The job exits non-zero and names the non-canonical file | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:project-shared-state:E001-SMOKE-001-cli-projects-real-store | planned — train:object-conflict-resolution:project-state (M1), RED (not yet implemented) | Projection files appear under .atdd/state/projection/ named by uid | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:reconcile-local-store:K001-SMOKE-001-collaboration-flow-runs-against-bare-remote | planned — train:object-conflict-resolution:project-state (M2), RED (not yet implemented) | Every command is a real subprocess invocation of the installed atdd CLI | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:enforce-merge-authority:C001-SMOKE-001-illegal-transition-merge | planned — train:object-conflict-resolution:project-state (M3), RED (not yet implemented) | illegal-transition-merge is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:enforce-merge-authority:C002-SMOKE-001-trailer-projection-mismatch | planned — train:object-conflict-resolution:project-state (M3), RED (not yet implemented) | trailer-projection-mismatch is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:enforce-merge-authority:C003-SMOKE-001-history-secret-leak | planned — train:object-conflict-resolution:project-state (M3), RED (not yet implemented) | history-secret-leak is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:enforce-merge-authority:C004-SMOKE-001-advisory-disposition-on-a-new-rule | planned — train:object-conflict-resolution:project-state (M3), RED (not yet implemented) | advisory-disposition-on-a-new-rule is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:enforce-merge-authority:D001-SMOKE-001-commit-trailer-schema | planned — train:object-conflict-resolution:project-state (M3), RED (not yet implemented) | commit-trailer-schema is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:enforce-merge-authority:D002-SMOKE-001-required-check-policy | planned — train:object-conflict-resolution:project-state (M3), RED (not yet implemented) | required-check-policy is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:enforce-merge-authority:E001-SMOKE-001-commit-trailer-parse | planned — train:object-conflict-resolution:project-state (M3), RED (not yet implemented) | commit-trailer-parse is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:govern-projection-fields:C001-SMOKE-001-policy-coverage-gap | planned — train:object-conflict-resolution:project-state (M4), RED (not yet implemented) | policy-coverage-gap is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:govern-projection-fields:C002-SMOKE-001-merge-matrix-blind-spot | planned — train:object-conflict-resolution:project-state (M4), RED (not yet implemented) | merge-matrix-blind-spot is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:govern-projection-fields:D001-SMOKE-001-field-ownership-policy | planned — train:object-conflict-resolution:project-state (M4), RED (not yet implemented) | field-ownership-policy is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:govern-projection-fields:D002-SMOKE-001-single-owner-body-rule-computability | planned — train:object-conflict-resolution:project-state (M4), RED (not yet implemented) | single-owner-body-rule-computability is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:govern-projection-fields:E002-SMOKE-001-safe-projection-merge | planned — train:object-conflict-resolution:project-state (M4), RED (not yet implemented) | safe-projection-merge is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:govern-projection-fields:K001-SMOKE-001-tombstoned-object-resurrection | planned — train:object-conflict-resolution:project-state (M4), RED (not yet implemented) | tombstoned-object-resurrection is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:govern-projection-fields:R001-SMOKE-001-unsafe-merge-conflict | planned — train:object-conflict-resolution:project-state (M4), RED (not yet implemented) | unsafe-merge-conflict is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:isolate-provider-boundary:C001-SMOKE-001-core-provider-import | planned — train:object-conflict-resolution:project-state (M5), RED (not yet implemented) | core-provider-import is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:isolate-provider-boundary:C002-SMOKE-001-provider-free-workflow | planned — train:object-conflict-resolution:project-state (M5), RED (not yet implemented) | provider-free-workflow is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:isolate-provider-boundary:D001-SMOKE-001-sync-provider-interface | planned — train:object-conflict-resolution:project-state (M5), RED (not yet implemented) | sync-provider-interface is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:isolate-provider-boundary:E001-SMOKE-001-provider-registration | planned — train:object-conflict-resolution:project-state (M5), RED (not yet implemented) | provider-registration is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:migrate-projection-authority:C001-SMOKE-001-lossy-migration-write | planned — train:object-conflict-resolution:project-state (M8), RED (not yet implemented) | lossy-migration-write is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:migrate-projection-authority:D001-SMOKE-001-migration-runbook | planned — train:object-conflict-resolution:project-state (M8), RED (not yet implemented) | migration-runbook is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:migrate-projection-authority:M001-SMOKE-001-shadow-projection-report | planned — train:object-conflict-resolution:project-state (M8), RED (not yet implemented) | shadow-projection-report is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:migrate-projection-authority:P001-SMOKE-001-rollout-rollback-plan | planned — train:object-conflict-resolution:project-state (M8), RED (not yet implemented) | rollout-rollback-plan is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:migrate-projection-authority:Y001-SMOKE-001-github-hot-path-read | planned — train:object-conflict-resolution:project-state (M8), RED (not yet implemented) | github-hot-path-read is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:migrate-projection-authority:Y002-SMOKE-001-manifest-read-fallback | planned — train:object-conflict-resolution:project-state (M8), RED (not yet implemented) | manifest-read-fallback is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:project-shared-state:C001-SMOKE-001-nondeterministic-projection-content | planned — train:object-conflict-resolution:project-state (M1), RED (not yet implemented) | nondeterministic-projection-content is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:project-shared-state:E002-SMOKE-001-projection-hydration | planned — train:object-conflict-resolution:project-state (M1), RED (not yet implemented) | projection-hydration is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:project-shared-state:E003-SMOKE-001-projection-digest | planned — train:object-conflict-resolution:project-state (M1), RED (not yet implemented) | projection-digest is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:project-shared-state:E004-SMOKE-001-uid-mint | planned — train:object-conflict-resolution:project-state (M1), RED (not yet implemented) | uid-mint is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:project-shared-state:Y001-SMOKE-001-slug-rename | planned — train:object-conflict-resolution:project-state (M1), RED (not yet implemented) | slug-rename is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:project-shared-state:Y002-SMOKE-001-rename-addressability | real (throwaway git checkout + real .atdd/state/state.sqlite, driven by subprocess through the installed CLI; HOME pinned inside tmp_path so the shared Control Root store is unreachable; no mocks, no patching) | `atdd state object rename` addresses a work item held under each live slug uid form — `unverified:<slug>` and bare slug — exits zero, and moves `data.title` and the body's leading H1 together | single direction (rename writes display metadata; no downstream handoff) | #1653 — the verb addressed 0 of 822 live work items. NOTE: this row records that the test executes against real infrastructure, NOT that a gate verified it; #1653's SMOKE obligation is documented vacuous because the store's `data.feature`/`data.train` are NULL and no sanctioned command sets them |
| acc:reconcile-local-store:C001-SMOKE-001-dirty-store-overwrite | planned — train:object-conflict-resolution:project-state (M2), RED (not yet implemented) | dirty-store-overwrite is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:reconcile-local-store:E001-SMOKE-001-overlay-event-log | planned — train:object-conflict-resolution:project-state (M2), RED (not yet implemented) | overlay-event-log is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:reconcile-local-store:M001-SMOKE-001-head-change-reconcile | planned — train:object-conflict-resolution:project-state (M2), RED (not yet implemented) | head-change-reconcile is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:reconcile-local-store:P001-SMOKE-001-store-base-commit | planned — train:object-conflict-resolution:project-state (M2), RED (not yet implemented) | store-base-commit is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:reconcile-local-store:P002-SMOKE-001-cold-start-hydrate | planned — train:object-conflict-resolution:project-state (M2), RED (not yet implemented) | cold-start-hydrate is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:reconcile-local-store:R001-SMOKE-001-overlay-replay | planned — train:object-conflict-resolution:project-state (M2), RED (not yet implemented) | overlay-replay is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:reconcile-local-store:R002-SMOKE-001-conflict-report | planned — train:object-conflict-resolution:project-state (M2), RED (not yet implemented) | conflict-report is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:reconcile-local-store:Y001-SMOKE-001-overlay-event-replayed-twice | planned — train:object-conflict-resolution:project-state (M2), RED (not yet implemented) | overlay-event-replayed-twice is observed to hold end-to-end against real infrastructure, with no mocks or manual patching | planned (authored by #1400; real-infrastructure classification filled at GREEN) | #1400 |
| acc:govern-lifecycle:E068-SMOKE-001-live-claude-md-contains-no-atdd-skip-references | real (grep -E 'ATDD_SKIP_[A-Z_]+' over the deployed repo-root CLAUDE.md) | zero ATDD_SKIP_* bypass tokens present in the agent-context file | filesystem read of CLAUDE.md | #1483 (rehomed from spawn-agents; operator-safety invariant survives the sub-worker-orchestration prune) |
| acc:govern-lifecycle:E065-SMOKE-001-live-claude-md-line-count-within-budget | real (line count of the deployed repo-root CLAUDE.md) | CLAUDE.md within the operator line budget while retaining lifecycle/command pointers | filesystem read of CLAUDE.md | #1483 (rehomed from spawn-agents) |
| acc:govern-lifecycle:E066-SMOKE-001-live-operator-emergency-bypass-doc-present-and-correct | real (reads docs/operator-emergency-bypass.md on disk) | doc present and documents the CLI, not an env-var escape hatch | filesystem read of docs/operator-emergency-bypass.md | #1483 (rehomed from spawn-agents) |
| acc:govern-lifecycle:R007-SMOKE-001-atdd-validate-coach-includes-size-budget-rule | real (`atdd validate coach` over the live repo) | the claude_md size-budget rule fires on the coach validator surface | CLI → coach validator registry → claude_md_validators | #1483 (rehomed from spawn-agents) |
| acc:govern-lifecycle:R008-SMOKE-001-atdd-validate-coach-includes-no-bypass-advertising-rule | real (`atdd validate coach` over the live repo) | the no-bypass-advertising rule fires on the coach validator surface | CLI → coach validator registry → claude_md_validators | #1483 (rehomed from spawn-agents) |
| acc:govern-lifecycle:E067-SMOKE-001-live-freedom-layer-passes-flipped-validator | real (flipped freedom-set validator evaluated against the deployed session.convention.yaml on disk) | zero violations: no allowed_bash entry pre-authorizes a forbidden command, every entry is tightly scoped Bash(<cmd>:*) | session.convention.yaml::spawn_time.freedom_layer -> policy/freedom_layer_bash_scope variant | #1483 (rehomed from spawn-agents; the policy variant and the freedom_layer data are still live, so the fault-injection guard is preserved rather than retired) |
| acc:govern-lifecycle:E069-SMOKE-001-live-smoke-run-attests-and-opens-the-gate | real (a genuine git repository with real history, a real pytest subprocess auto-loading the substrate plugin through materialized pytest11 `.dist-info` metadata rather than a harness `-p` flag, a real SQLite State Store and the real SmokeExecutionGateCheck; nothing between the test running and the gate deciding is stubbed, and the harness itself cannot write an attestation — it can only run pytest and then look) | the executing run writes exactly one attestation (outcome passed, non-zero measured duration, commit_sha == the fixture repo's HEAD, execution_kind live_smoke, the acceptance URN it discharges) and the gate refuses SMOKE→REFACTOR before it and passes after — so the attestation, not a typed stamp, is what opened the gate; the negative controls (a skipped live-smoke test, and one that never runs despite a green suite) record a non-passing outcome and leave the gate closed | atdd.coach.gate.live_smoke harness → real pytest subprocess (pytest11 entry point) → attestation hook write → State Store → SmokeExecutionGateCheck verdict | #1602 (the repo's first `execution_kind: live_smoke` acceptance; without it the #1602 gate is a no-op that no run can satisfy and SMOKE→REFACTOR is reachable only by --force, and E060's constant-evidence gate has no subject to police. Anchored to `src/atdd/coach/gate/tests/test_1602_smoke_execution_end_to_end.py`) |
| acc:govern-lifecycle:D020-SMOKE-001-shipped-package-loads-the-declared-machine | real (two tiers, no worktree shortcut: a SEPARATE process running the real `load_conventions` and `_phase_machine_path`, PLUS a wheel built from this tree via the C008/C009 `_wheel_harness` and read back with NO source tree on sys.path, so a data file that did not ship is genuinely absent rather than shadowed by the checkout) | the built wheel CONTAINS both `phase_machine.convention.yaml` and the transition-autonomy node as package data; a consumer-shaped load off the unpacked wheel yields the pinned autonomy table on all nine phases, and the conventions snapshot hash is unmoved from `88af3062…` so installing it cannot invalidate an in-flight run | real process -> `_phase_machine_path` packaged-copy branch (in-repo copy absent from the path) -> `load_conventions` -> PhaseSpec + `_normalized_snapshot`; wheel via `python -m build` -> `wheel_members` / `extracted_wheel_root` | #1626 (Track 0 of #1627. The RED-tier narrowing is RETIRED: the shipped-artifact clause is now asserted against a real wheel. Fault-injected 2026-07-28 — stripping `autonomy` from the unpacked wheel makes the consumer probe report NONE and the assertion fire while still resolving from the wheel, so the guard is load-bearing, not a stub. Note the INSTALLED atdd 4.27.0 predates this change and carries neither file; asserting against it would assert 'my unmerged change has been released', which is unsatisfiable at SMOKE) |
| acc:govern-lifecycle:E070-SMOKE-001-innocent-branch-not-blocked-by-a-sibling-offender | real (a real git repository with the branch checked out, so `PRManager._detect_branch` runs a real `git rev-parse`; a real stub `gh` executable on PATH, so `_existing_pr_for_branch` runs a real subprocess and the URL-to-number parsing that was the defect is exercised end to end; no collaborator is substituted) | on the push-event path (`GITHUB_REF=refs/heads/<branch>`), the pre-SMOKE close gate blocks only the PR under validation: an innocent branch is not failed by a sibling PR's offense, while the offender's own run still fails the strict gate | both directions (innocent passes, offender still blocked) | #1478; the live outage was CI run 29235575570 (push, FAILED on PR #1461's violation) vs 29235577497 (same commit, pull_request, PASSED) |
| acc:govern-lifecycle:Y006-SMOKE-001-real-cli-revise-populates-the-store | real (the actual `atdd author issue --revise` CLI as a subprocess against a real on-disk control-root State Store, with the GitHub projection unreachable so the outbox path is taken; no in-process import of the module under test, no monkeypatching) | after the real CLI runs, the work item read back by a SEPARATE process reports `data.feature` equal to the URN the operator supplied — and a URN that resolves to nothing in plan/ exits non-zero leaving the stored feature unchanged | `atdd author issue --revise <N> --feature <urn>` (subprocess) → `revise_issue` → `revise_work_item_issue` → store → separate-process read-back | #1635 (the fourth break, measured 2026-07-28: across eight issues the body metadata table updated on all eight while `work_item.data.feature` stayed NULL on all eight, because `--revise` accepts `--feature` and the revise call chain carries only body and issue_type. Same silent-ignore family as #1631 (`--body-file` dropped on create). An in-process unit would prove the seam threads the argument but not that the shipped entry point does — which is exactly the gap that made the flag look functional) |
| acc:govern-lifecycle:Y007-SMOKE-001-real-cli-revise-writes-the-title | real (the actual `atdd author issue --revise` CLI as a subprocess against a real on-disk control-root State Store, with a real `gh` on PATH that records its argv so the projection resolves and is observable without touching production GitHub; no in-process import of the module under test) | after the real CLI runs, the work item read back reports `data.title` equal to the string the operator supplied, and the recorded `gh` argv contains a title edit for that issue number — and a flag the revise path cannot honour exits non-zero with no `gh` call made and the stored body unchanged | `atdd author issue --revise <N> --title <t>` (subprocess) -> `revise_issue` -> `revise_work_item_issue` -> store, and -> `integrations.github.issue_state.update_title` (projection) -> `sync.enqueue_outbox` on failure | #1661 (measured 2026-08-02: `--revise --title X --type bug` exited 0 with `data.title` unchanged and zero GitHub title calls; with `--body-file` alongside it the body's H1 moved while the issue title stayed stale, which is the #1636 divergence an operator repaired by hand. A recording stub rather than an unreachable one is required here: an absent `gh` makes every projection fail identically, so it cannot distinguish 'issued a title edit that failed' from 'never issued one' — and the second is the defect) |
| acc:govern-lifecycle:C011-SMOKE-001-real-validate-coach-reports-the-binding | real (the real `atdd validate coach --local` entry point as a subprocess over a real checkout; the validator is discovered and selected the way CI discovers it, with no monkeypatching of the validator or its plan/ resolver) | the validator's identifier appears in the run (so it is not silently deselected by the gate's marker expression), the non-resolving binding is reported naming the issue and the URN, and the resolving binding is not | `atdd validate coach --local` (subprocess) → validator discovery/selection → feature-binding scan → plan/ resolution → disposition gate | #1635 (a validator that the shipped gate never selects enforces nothing; this repo's own planner gate deselects `platform`-marked validators, so selection is asserted rather than assumed. Outcome honours the rule's declared disposition, so severity stays governed by the convention) |
| acc:govern-lifecycle:C015-SMOKE-001-real-cli-refuses-to-report-a-stalled-queue-as-drained | real (the real `atdd state sync --push` entry point against a real migrated State Store under an isolated Control Root, with real pending outbox rows written through the store's own writer and the provider registry left as this repository ships it — empty; nothing about the store, the engine or the registry monkeypatched) | the command exits non-zero over a queue it did not drain and names both the verdict and the unregistered provider, no row is marked sent, and the same command over the same store exits 0 once a provider that accepts the operation is registered | `atdd state sync --push` (real entry point) → `discover_providers` → `push_outbox` → outbox verdict → exit code | #1711 (measured 2026-08-03 against the live store: 30 rows pending, oldest 25 days, exactly 2 ever sent, `discover_providers()` == {} — and the drain reported `0 pushed, 0 failed` and exited 0 the whole time. The registered-provider leg is the discriminator: without it a non-zero exit cannot be distinguished from the flag simply refusing) |
| acc:govern-lifecycle:L003-SMOKE-001-coach-enter-lists-plan-backed-wmbts | real (the actual `atdd coach enter <N>` CLI as a subprocess against a real State Store and a real plan/ tree, run with NO GitHub credentials available so a lookup still depending on `gh` would fail rather than quietly return an empty list) | the bound issue's output lists the WMBT URN its feature YAML declares, with no credentials present (proving resolution came from plan/, not the provider); the unbound issue reports the missing binding explicitly and does not print "none found" | `atdd coach enter <N>` (subprocess) → stored feature binding → feature YAML `wmbts:` in plan/ → rendered WMBT list | #1635 (closes the user-visible symptom: `_fetch_sub_issues` shells out to `gh issue list --label atdd-wmbt` and never reads plan/, so every issue in the repo reported "WMBTs: none found" regardless of decomposition. The absent-credentials condition is the discriminator — the old lookup failed silently to an empty list, which is indistinguishable from a correct empty answer) |
| acc:govern-lifecycle:C013-SMOKE-001-real-store-not-applicable-yields-the-new-verdict | real (a real migrated State Store on disk under an isolated Control Root, with a work item registered and projected from a GitHub issue number the way the live path registers one; the real `SmokeExecutionGateCheck` run through its real entry point, no monkeypatching of the store, the obligation resolver or the verdict, and the negative control's attestation written through the real producer API) | the migrated not-applicable branch reports the NOT_APPLICABLE verdict rather than a bare pass; `evaluate_gate` still proceeds for that issue, so the transition outcome is unchanged; the legacy `passed` bool still reads True for existing callers; and an issue that DID declare a live_smoke acceptance with no attestation still reports FAIL and still blocks | store `external_refs` projection (issue number → uid) → `work_item_data` → `live_smoke_obligation` over the real plan/ tree → `SmokeExecutionGateCheck.run` → `GateCheckResult.verdict` → `evaluate_gate` partition | #1719 (the vocabulary correction; SMOKE->REFACTOR is enabled in .atdd/config.yaml and this check is registered for it, so the no-outcome-change half is the safety property, not a nicety — a blocking migration would strand every issue declaring no live_smoke acceptance behind --force. The negative control is what stops "NOT_APPLICABLE everywhere" from satisfying the row) |
| acc:govern-lifecycle:R010-SMOKE-001-coach-approve-token-and-gate-share-one-control-root-location | mixed — REAL command and REAL gate over a SYNTHETIC filesystem: the actual `approve_command.run` mints and the real `register_approval_checks` -> `GateRegistry` -> `evaluate_transition_gate` -> `ApprovalTokenGateCheck` decides, with no mocks, stubs or monkeypatching anywhere on the mint or gate path; but the Control Root and its child worktree are a marker-based temp layout under `tmp_path` and the issue number is a throwaway (999999), so no live issue, no live Control Root and no GitHub or label I/O is touched | the token minted while standing in the CHILD worktree materializes under the SHARED Control Root and NOT under the child; the gate evaluating from that same child then resolves that token and proceeds; a SIBLING worktree of the same project resolves the identical receipt (impossible before this fix); and scope isolation survives the move, so the PLANNED->RED receipt still refuses RED->GREEN | both directions — mint (`approve_command.run` -> `approval_token_path` -> `resolve_operational_root` -> Control Root) and check (`evaluate_transition_gate` -> `ApprovalTokenGateCheck.run` -> `locate_approval_token` -> Control Root, worktree-local fallback); a second case pins the degrade path so a single-repo layout with no resolvable Control Root above it still mints and checks in place | #1376 (child of program #1670). The gate joined `approval_relpath` onto `ctx.worktree` — the literal cwd `IssueLifecycle._transition_gate` passes — while every other operational `.atdd/` reader has resolved through `resolve_operational_root` since #1346, so a receipt was invisible to the gate that needed it (measured in the #1307 walk). Synthetic-filesystem risk per #855 is mitigated out-of-band: the same resolution was additionally exercised against the REAL Control Root in both directions — Control-Root token preferred with `legacy=False` while a worktree-local twin was present (precedence, not merely unused fallback), and the worktree fallback honoured with an observable warning when it was not |
| acc:govern-lifecycle:C014-SMOKE-001-the-real-runner-reports-a-real-deselection | real (the real `TestRunner` against this repo's real `src/atdd/<phase>/validators/` trees, with the marker injection and the counting unmonkeypatched; the reference count is taken from a real serial `pytest` run of the same selection) | the run's reported could-not-check count is > 0 and equals what pytest reports as "N deselected" when the identical selection is run without xdist; the count appears in the run's output, which it does not today; the exit code is unchanged | `TestRunner.run_tests` → consumer-mode `not platform` injection → `--collect-only` probe → `CoverageReport` → printed report + baseline field | #1632 (measured 2026-08-03: `atdd validate planner --local --skip-api` prints "237 passed" while serially the same selection prints "237 passed, 208 deselected" — the xdist controller never sees `pytest_deselected`, so the gate's own default parallelism erases pytest's only notice; 812 of 1898 across all four phases) |
| acc:govern-lifecycle:C016-SMOKE-001-real-cli-admits-the-commit-on-a-divergent-branch | mixed — REAL `atdd coach is-registered` resolution path and a REAL State Store over a SYNTHETIC control root: the store is seeded through the real storage API (`ObjectStore.upsert`) under a `tmp_path` Control Root with throwaway uids, so no live issue, no live Control Root and no GitHub I/O is touched; nothing on the branch-resolution path is mocked, stubbed or monkeypatched | a registered work item whose `data.branch` diverges from its uid resolves as registered and exits 0, so the pre-commit hook admits the commit; a branch bound to no work item in the same populated store still exits non-zero, so the gate is repaired rather than widened into a pass; and neither invocation reads `.atdd/manifest.yaml`, so the retired fallback is not quietly reinstated | pre-commit hook → `atdd coach is-registered "$BRANCH"` → `IssueManager.branch_is_registered` → State Store `data.branch` binding (the same field `BranchManager._record_binding_in_store` writes at worktree-create time, #1347, and `atdd worktree list` already reads) → exit code | #1720 (child of program #1670). Measured 2026-08-03 against the live Control Root: 876 work items, 304 carrying a populated `data.branch`, and 206 of those 304 — the majority — have a branch slug that is not their uid, so the refusal is the common case not an edge; `atdd coach is-registered feat/resolve-approval-token-control-root` exits 1 for #1376's fully registered work. The unregistered-branch leg is what stops "registered everywhere" from satisfying the row, and the manifest assertion is what keeps the fix one source with two indexes rather than the dual-source fallback #1400 CORE-034 retired |
| acc:govern-lifecycle:C021-SMOKE-001-real-mint-refuses-a-real-unsatisfied-obligation | mixed — the REAL `atdd coach approve` entry point, the REAL registrars the transition dispatch calls, the REAL `SmokeExecutionGateCheck` and a REAL migrated State Store over a SYNTHETIC Control Root: the store is seeded through the real storage API under a `tmp_path` Control Root with throwaway uids and issue numbers, so no live issue, no live Control Root and no GitHub I/O is touched; nothing on the mint or the gate path is mocked, stubbed or monkeypatched | a work item whose plan scope declares an `execution_kind: live_smoke` acceptance and has no attestation refuses the mint, exits non-zero, and leaves NO token file anywhere under the Control Root — so the artifact the enforcing gate accepts as authorisation was never created; the output names the clause the smoke verdict refused on; a work item declaring no live_smoke acceptance mints and exits 0, so the refusal is the unmet obligation and not the command becoming unusable; and a PLANNED->RED mint on the same store is unaffected in both cases, so the conditional behaviour is confined to the one edge that has something to consult | `atdd coach approve <N> --transition SMOKE->REFACTOR` → `register_approval_checks` + `register_smoke_execution_check` → `GATE_REGISTRY.checks_for` → approval-check exclusion → `run_checks` → `evaluate_gate` → mint-or-refuse → filesystem | #1670 slice C (the program's central deliverable). Measured 2026-08-03 on #1726: all five of its transitions were REFUSED by the template gate while both `atdd coach approve` calls SUCCEEDED, leaving `PLANNED-RED.json` and `SMOKE-REFACTOR.json` on the shared Control Root for an issue that never left INIT. Scoped to SMOKE->REFACTOR ALONE because `registrations.py` binds a substantive check to that edge and no other; on the four bare edges the mint would consult only `ApprovalTokenGateCheck`, signing a receipt for its own existence. The not-applicable leg is the safety property and not a nicety — 787 of 787 work items declared no live_smoke acceptance when the edge was enabled, so refusing on NOT_APPLICABLE would strand the repo at SMOKE behind --force; the declaring leg is what stops "mints everywhere" from satisfying the row |
| acc:govern-lifecycle:C023-SMOKE-001-the-real-drift-check-carries-the-warning-to-the-operator | mixed — the REAL `RegistryBuilder`, the REAL drift detection, the REAL check-mode dispatch and the REAL `format_fix_hint` over a SYNTHETIC filesystem: nothing on the detect-or-report path is mocked, stubbed or monkeypatched, but the repository is a `tmp_path` fixture holding one wagon manifest and its mirror, so no live repo, no git history and no GitHub I/O is touched | the run reports drift and its output carries BOTH the `## Artifacts` consequence and the `git diff --name-only origin/main..HEAD` re-derivation, so the warning reaches the operator through the surface actually read rather than only through a direct call to the formatter; the drifted mirror is named, so the warning is about the files the prescribed amend will really append; and the same run over a repo whose mirror matches its source reports in-sync and emits neither the warning nor the amend, so drift is the discriminator and not an unconditional branch | `RegistryBuilder.update_wagon_registry(mode="check")` → `_detect_changes` → `_confirm_and_apply` check branch → `format_fix_hint` → stdout | #1739 (child of program #1670). The remediation's own `git add plan/_wagons.yaml plan/_trains.yaml contracts/_artifacts.yaml && git commit --amend --no-edit` appends up to three mirrors to a commit whose `## Artifacts` was authored from the pre-resync diff, so the toolchain manufactured the exact undeclared-path violation #1726 had just bound the REFACTOR -> COMPLETE gate to refuse — measured when it caught #1726's own worker mid-implementation. The in-sync leg is the load-bearing one: printing the warning unconditionally would satisfy the drift leg while putting an Artifacts lecture in front of every clean check, which is how a warning stops being read before the one run where it is true |
