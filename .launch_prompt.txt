You are an ATDD agent implementing **#583** — make pre-push hook block on validator failures (currently advisory only).

## Worktree

You are in: `/Users/alecfokapu/Github/atdd/feat-583-prepush-validator`
Branch: `feat/583-blocking-prepush-validator` (tracks origin/main)
Base: commit fe82c7a — **Layer 1+2 of #629 just shipped here**; the pre-push hook already has the bare-mode block.

## Hazard zone — READ THIS FIRST

The prior agent on this issue shipped PR #627 with **219,590 line DELETIONS** due to `core.bare=true` contamination. PR #627 closed unrecoverable.

**Two new git hooks are LIVE on main and will block contamination:**
- `pre-push`: blocks `core.bare=true` (override: `ATDD_SKIP_BARE_CHECK=1` — don't)
- `commit-msg`: blocks mass-delete diff unless decom prefix

But ALSO be paranoid manually. Before every commit + push:
1. `git config core.bare` → must be false/unset
2. `git diff --cached --stat | tail -1` → sane deletion count
3. `git diff --stat origin/main..HEAD | tail -1` → sane total

NEVER `git add -A` or `git add .` — use specific filenames.

## What to build

Run `gh issue view 583 --repo afokapu/atdd --json body --jq '.body'` for full context. Short version:

The existing `pre-push` hook (in `src/atdd/coach/templates/hooks/pre-push`) is advisory-only — it warns on uncommitted-file counts but doesn't run `atdd validate coder` / `atdd validate coach`. Agents repeatedly trip silent-swallow + boundary + rule-id-coherence validators in CI **after** push, burning 1-2 CI cycles per PR.

### Scope

Add a BLOCKING fast-validator pass to pre-push:
- Run only **local, network-free** validators (the same `--local --skip-api` set that the post-commit hook uses today: planner/tester/coder/coach).
- Run only the **blast-radius** for files changed since `origin/main` — same path→phase mapping the post-commit hook uses (already in CLAUDE.md::git::post_commit_hook::path_to_phase_mapping).
- Exit non-zero on any validator failure with a clear message + recovery hint.
- Override: `ATDD_SKIP_PREPUSH_VALIDATE=1 git push ...` for emergencies.

### Compose with existing pre-push checks

The current pre-push hook has, in order:
1. `set -e`
2. **NEW**: bare-mode block (from PR #630 — don't touch)
3. Version gate
4. Tag-push block
5. Main/master push block
6. Uncommitted delta warning (advisory)

Add the validator pass **after the main-block** and **before the uncommitted warning**.

## ATDD discipline

1. `atdd gate` → confirm.
2. **Planner**: extend or add a WMBT under the wagon that owns hooks. Look at `plan/integration_hardening/C002.yaml` (just shipped by PR #630) — the new acceptance(s) should follow the same pattern.
3. **Tester (RED)**: failing tests in `src/atdd/coach/templates/hooks/tests/` (next to the C002 tests from PR #630). Pattern: tmp_path git repo with an installed pre-push hook + a known-bad file (e.g. silent-swallow) — verify push is blocked. Plus a known-good file → not blocked.
4. **Coder (RED→GREEN)**: extend `src/atdd/coach/templates/hooks/pre-push` per scope. Mirror to `.atdd/hooks/pre-push` (SPEC-SESSION-VAL-0084 — `validate-coach::test_installed_hooks_match_templates`).
5. Don't forget to also bump `wmbt.total` and add the new acceptance entries in `_integration_hardening.yaml`. PR #630 hit this CI failure — learn from it.
6. Version bump: MINOR (new hook behavior).
7. Micro-commits per phase.

## Commit hygiene

- `git status -s`, `git add <specific>`, NEVER `-A`/`.`
- Before commit: `git diff --cached --stat`
- Before push: `git diff --stat origin/main..HEAD | tail -1`

## PR

`atdd pr 583` — NEVER `gh pr create`.

## Stop conditions

PR opened, CI started → wait + report.

GO. Start with `atdd gate`.
