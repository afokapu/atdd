{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "frontend:train:render-metadata",
  "title": "FrontendTrainRenderMetadata",
  "description": "Optional render-content expectations attached to a train manifest. Consumed by tester validator test_train_renders_content.py to verify that mounting the train produces non-empty, non-stub DOM output. SPEC-CODER-TRAIN-0003.",
  "version": "1.0.0",
  "x-artifact-metadata": {
    "domain": "frontend",
    "resource": "train",
    "version": "1.0.0",
    "hash": "",
    "producer": "wagon:govern-lifecycle",
    "consumers": ["wagon:govern-lifecycle"],
    "dependencies": [],
    "api": {
      "operations": [
        {
          "method": "GET",
          "path": "/plan/_trains/{train_id}.yaml",
          "description": "Validator reads optional render-metadata block from a train manifest before mounting via FrontendTrainRunner.runTrain.",
          "responses": {
            "200": {
              "description": "Render metadata block (or absent — treated as no expectations).",
              "schema": "$ref: #"
            }
          }
        }
      ]
    },
    "traceability": {
      "wagon_ref": "plan/govern_lifecycle/_govern_lifecycle.yaml",
      "feature_refs": [
        "feature:govern-lifecycle:enforce-train-has-rendered-content"
      ]
    },
    "governance": {
      "status": "draft"
    }
  },
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "train_id": {
      "type": "string",
      "description": "The train identifier (matches plan/_trains.yaml entry)."
    },
    "expected_content": {
      "type": "object",
      "description": "What 'rendered' means for this train. When omitted, the validator only requires textLength > 0 and stubDetected == false.",
      "additionalProperties": false,
      "properties": {
        "min_text_nodes": {
          "type": "integer",
          "minimum": 1,
          "description": "Minimum number of non-empty text nodes the rendered DOM must contain."
        },
        "must_contain": {
          "type": "array",
          "description": "Substrings or selectors that must be present in the rendered DOM.",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true
        },
        "fixture": {
          "type": "string",
          "description": "Optional path to a fixture loaded into the harness before mounting (relative to repo root)."
        }
      }
    },
    "template": {
      "type": "string",
      "description": "Optional layout template the train resolves through TrainView."
    },
    "auth_required": {
      "type": "boolean",
      "description": "When true, the harness mounts the train inside an authenticated session fixture."
    }
  }
}
