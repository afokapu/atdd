{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "commons:provider-capability",
  "title": "Provider implementation realization index",
  "description": "Provider implementation realization index exchanged inside train 0007-enforce-extension-conventions.",
  "version": "1.0.0",
  "type": "object",
  "properties": {
    "rule_id": {
      "type": "string"
    },
    "disposition": {
      "type": "string"
    }
  },
  "required": [
    "rule_id"
  ],
  "additionalProperties": false,
  "x-artifact-metadata": {
    "domain": "commons",
    "resource": "provider_capability",
    "api": {
      "version": "v1",
      "operations": [
        {
          "method": "GET",
          "path": "/contracts/commons/provider-capability.schema.json",
          "description": "Read the Provider implementation realization index payload contract.",
          "responses": {
            "200": {
              "description": "The payload contract schema.",
              "schema": "$ref: #"
            }
          }
        }
      ]
    },
    "producer": "wagon:govern-providers",
    "consumers": [
      "wagon:bind-extension-conventions"
    ],
    "dependencies": [],
    "traceability": {
      "wagon_ref": "plan/govern_providers/_govern_providers.yaml",
      "feature_refs": [
        "feature:govern-providers:govern-providers"
      ]
    },
    "governance": {
      "status": "draft"
    }
  }
}
