schema_version: 1.1.0
rule_id: coach.execution.structured-correction-and-escalation
kind: principle
status: active
name: Corrections and escalations use structured channels
statement: Corrections to an agent must use the structured correction channel, never ad-hoc multiplexer
  slash-command injection; deny-pattern actions route to the structured escalation channel rather than
  modal typing.
source:
  legacy_path: src/atdd/coach/conventions/session.convention.yaml
  legacy_section: spawn_time.deny_pattern_escalation + leash_layer + slash_command_prohibition
  legacy_sha: dc30019cdcf8872b
  extraction_mode: high_fidelity
metadata:
  severity: 3
  disposition: documentation-only
  introduced_in: 3.84.0
parity:
  source_fragments_preserved: true
  examples_preserved: false
  implementation_preserved: false
  fix_hint_preserved: false
  reviewed_at: '2026-06-17'
terms:
- term_id: structured_channel
  text: the correction inbox + escalation record the coach consumes; replaces runtime /slash-command injection
    (M001).
