schema_version: 1.0.0
rule_id: coder.coverage.every-feature-must-have
kind: rule
status: active
name: Feature - Implementation Coverage
statement: Every feature must have corresponding code implementation
terms:
- term_id: feature_implementation_coverage
  text: A feature is covered when each of its components maps to a code path in python/,
    supabase/, or web/.
implementation:
  type: validator
  ref: conventions/coverage/test_hierarchy_coverage::test_fault_injection_convention_catches_legacy_warn_only
source:
  legacy_path: src/atdd/coder/conventions/coverage.convention.yaml
  legacy_section: rules
  legacy_rule_id: coder.coverage.every-feature-must-have
  extraction_mode: high_fidelity
content:
  normative_text: Each feature's components must map to code paths
  exceptions: features_without_implementation allow-list
metadata:
  severity: 3
  disposition: strict
  aliases:
  - COVERAGE-CODE-4.1
