schema_version: 1.0.0
rule_id: coder.error-response.bare-string
kind: rule
status: active
name: No bare string error detail
statement: HTTPException detail must be a structured dict matching the error response
  contract, not a bare string
terms:
- term_id: structured_error_detail
  text: An HTTPException detail must be a dict matching contracts/commons/error/response.schema.json,
    never a bare string.
implementation:
  type: validator
  ref: test_error_response_compliance::test_error_response_contract_exists
source:
  legacy_path: src/atdd/coder/conventions/error-response.convention.yaml
  legacy_section: rules
  legacy_rule_id: coder.error-response.bare-string
  extraction_mode: high_fidelity
metadata:
  severity: 4
  disposition: strict
  aliases:
  - ERROR-BARE-STRING-001
  introduced_in: 1.67.0
