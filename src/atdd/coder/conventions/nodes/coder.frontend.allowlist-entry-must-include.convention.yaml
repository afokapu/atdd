schema_version: 1.0.0
rule_id: coder.frontend.allowlist-entry-must-include
kind: rule
status: active
name: Allowlist entry must include migration reference
statement: 'Allowlist entry must include a migration: field referencing the issue
  that will eliminate the exemption.'
terms:
- term_id: migration_reference
  text: 'A migration: field on a no-stub-presentation allowlist entry that references
    the owner/repo#NNN issue which will eliminate the exemption.'
source:
  legacy_path: src/atdd/coder/conventions/frontend.convention.yaml
  legacy_section: frontend.no_stub_presentation.rules
  legacy_rule_id: coder.frontend.allowlist-entry-must-include
  extraction_mode: high_fidelity
content:
  fix_hint: 'Add a migration: owner/repo#NNN field to the no_stub_presentation allowlist
    entry referencing the issue that will de-stub it.'
metadata:
  severity: 2
  disposition: documentation-only
  aliases:
  - PRESENTATION-NOSTUB-010
  introduced_in: 1.67.0
