schema_version: 1.0.0
rule_id: coder.frontend.boundaries-fe-imports
kind: rule
status: active
name: Frontend imports flow inward
statement: Frontend imports respect inward dependency direction (presentation -> application
  -> domain)
terms:
- term_id: inward_dependency
  text: "Frontend imports point inward \u2014 presentation depends on application,\
    \ application on domain \u2014 so inner layers never depend on outer ones."
source:
  legacy_path: src/atdd/coder/conventions/frontend.convention.yaml
  legacy_section: rules
  legacy_rule_id: coder.frontend.boundaries-fe-imports
  extraction_mode: high_fidelity
content:
  fix_hint: "Re-route the offending import so dependencies flow inward (presentation\
    \ \u2192 application \u2192 domain); never import an outer layer from an inner\
    \ one."
metadata:
  severity: 3
  disposition: documentation-only
  aliases:
  - BOUNDARIES-FE-IMPORTS-001
  introduced_in: 1.67.0
