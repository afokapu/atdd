components:
  backend:
    application:
    - count: 2
      rationale: register_provider() dedup-insert and discover_providers() deterministic
        ordered read
      type: use_cases
    integration:
    - count: 1
      rationale: the in-process provider registry holding registered SyncProvider
        instances by name
      type: stores
description: 'Provider registration and discovery seam: empty by default, deterministic
  name-sorted ordering, duplicate names rejected, and never consulted by any lifecycle
  decision (CORE-027).'
sizing:
  footprint_score: 5
  footprint_size: S
  wmbts: 1
urn: feature:isolate-provider-boundary:register-sync-providers
wagon: wagon:isolate-provider-boundary
wmbts:
- wmbt:isolate-provider-boundary:E001
