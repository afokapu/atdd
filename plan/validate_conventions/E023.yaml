urn: wmbt:validate-conventions:E023
step: execute
direction: minimize
dimension: likelihood
object_of_control: unverified-parity-claim
context_clarifier: no P0 pair is marked parity-verified without a passing legacy-vs-convention
  fault-injection diff
lens: functional.effectiveness
statement: minimize likelihood of unverified-parity-claim by blocking any parity-verified
  mark without a passing legacy-vs-convention fault-injection diff
acceptances:
- identity:
    urn: acc:validate-conventions:E023-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E023 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
