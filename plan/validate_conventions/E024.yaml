urn: wmbt:validate-conventions:E024
step: execute
direction: minimize
dimension: likelihood
object_of_control: unadjudicated-stricter-finding
context_clarifier: every convention finding legacy does not flag is classified real-gap,
  schema-bug, or loader-bug
lens: functional.effectiveness
statement: minimize likelihood of unadjudicated-stricter-finding by classifying every
  convention-only finding as real-gap, schema-bug, or loader-bug
acceptances:
- identity:
    urn: acc:validate-conventions:E024-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E024 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
