urn: wmbt:validate-conventions:E002
step: execute
direction: minimize
dimension: likelihood
object_of_control: registry-archetype-divergence
context_clarifier: each family archetype.py exposes exactly the template ids registry.yaml
  assigns it (registry<->code roundtrip)
lens: functional.effectiveness
statement: minimize likelihood of registry-archetype-divergence by ensuring each family
  archetype.py exposes exactly the template ids registry.yaml assigns to that family
acceptances:
- identity:
    urn: acc:validate-conventions:E002-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E002 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
