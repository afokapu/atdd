urn: wmbt:validate-conventions:E004
step: execute
direction: minimize
dimension: likelihood
object_of_control: malformed-family-folder
context_clarifier: every family folder contains README.md, archetype.py and fixtures.py
lens: functional.effectiveness
statement: minimize likelihood of malformed-family-folder by ensuring every family
  folder contains README.md, archetype.py and fixtures.py
acceptances:
- identity:
    urn: acc:validate-conventions:E004-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E004 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
