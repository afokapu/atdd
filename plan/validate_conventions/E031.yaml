urn: wmbt:validate-conventions:E031
step: execute
direction: minimize
dimension: likelihood
object_of_control: unmeasured-parity-delta
context_clarifier: a gap report quantifies parity, improvements, regressions, and
  false positives from the catch-matrix
lens: functional.effectiveness
statement: minimize likelihood of unmeasured-parity-delta by generating a gap report
  that quantifies parity, improvements, regressions, and false positives from the
  catch-matrix
acceptances:
- identity:
    urn: acc:validate-conventions:E031-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E031 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
