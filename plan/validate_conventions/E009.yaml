urn: wmbt:validate-conventions:E009
step: execute
direction: minimize
dimension: likelihood
object_of_control: unexplained-exclusion
context_clarifier: every file in the exclusion table carries an explicit reason
lens: functional.effectiveness
statement: minimize likelihood of unexplained-exclusion by ensuring every file in
  the exclusion table carries an explicit reason
acceptances:
- identity:
    urn: acc:validate-conventions:E009-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E009 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
