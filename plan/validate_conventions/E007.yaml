urn: wmbt:validate-conventions:E007
step: execute
direction: minimize
dimension: likelihood
object_of_control: invented-family-template
context_clarifier: 'every mapped entry uses one of the canonical families and templates
  from the #1204 registry, none invented'
lens: functional.effectiveness
statement: 'minimize likelihood of invented-family-template by ensuring every mapped
  entry uses a canonical family and template from the #1204 registry'
acceptances:
- identity:
    urn: acc:validate-conventions:E007-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E007 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
