urn: wmbt:validate-conventions:E036
step: execute
direction: minimize
dimension: time
object_of_control: convention-suite-ci-runtime
context_clarifier: the validate-conventions job installs pytest-xdist, runs the non-mutating
  subset under -n auto and the filesystem-mutation subset serially, and validate-gate
  fans in both results
lens: functional.efficiency
statement: minimize time of convention-suite-ci-runtime by ensuring the validate-conventions
  job installs pytest-xdist, runs the non-mutating subset under -n auto and the filesystem-mutation
  subset serially, and validate-gate fans in both results
acceptances:
- identity:
    urn: acc:validate-conventions:E036-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E036 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
