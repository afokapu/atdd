urn: wmbt:validate-conventions:E035
step: execute
direction: minimize
dimension: likelihood
object_of_control: unmarked-filesystem-mutation-test
context_clarifier: every convention test that writes, deletes, renames or creates
  a path carries the convention_filesystem_mutation marker, a guard forbids it from
  requesting the session clean graph, and a guard proves in-memory mutation leaves
  the session graph unchanged
lens: functional.effectiveness
statement: minimize likelihood of unmarked-filesystem-mutation-test by ensuring every
  convention test that writes, deletes, renames or creates a path carries the convention_filesystem_mutation
  marker, a guard forbids it from requesting the session clean graph, and a guard
  proves in-memory mutation leaves the session graph unchanged
acceptances:
- identity:
    urn: acc:validate-conventions:E035-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E035 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
