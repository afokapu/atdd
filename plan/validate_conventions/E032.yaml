urn: wmbt:validate-conventions:E032
step: execute
direction: minimize
dimension: time
object_of_control: clean-convention-graph-construction
context_clarifier: the composed convention graph is built once per pytest session
  and reused by every read-only baseline and contract test
lens: functional.efficiency
statement: minimize time of clean-convention-graph-construction by ensuring the composed
  convention graph is built once per pytest session and reused by every read-only
  baseline and contract test
acceptances:
- identity:
    urn: acc:validate-conventions:E032-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E032 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
