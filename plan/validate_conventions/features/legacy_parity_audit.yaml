components:
  backend:
    application:
    - count: 1
      rationale: map completeness + registry-consistency checker
      type: use_cases
    domain:
    - count: 1
      rationale: parity entry record (status enum + priority)
      type: value_objects
    integration:
    - count: 1
      rationale: legacy-validator-map.yaml machine-readable parity map
      type: schemas
    presentation:
    - count: 0
      rationale: pytest-collected meta-validator over the parity map
      type: controllers
description: 'The deterministic legacy->convention parity map: docs/validator-parity/convention-validator-parity-audit.md
  and legacy-validator-map.yaml mapping every legacy validator to family/template/variant/target/status/priority,
  plus a completeness meta-validator over the map.'
sizing:
  footprint_score: 5
  footprint_size: S
  wmbts: 4
urn: feature:validate-conventions:legacy-parity-audit
wagon: wagon:validate-conventions
wmbts:
- wmbt:validate-conventions:E006
- wmbt:validate-conventions:E007
- wmbt:validate-conventions:E008
- wmbt:validate-conventions:E009
