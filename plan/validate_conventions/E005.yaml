urn: wmbt:validate-conventions:E005
step: execute
direction: minimize
dimension: likelihood
object_of_control: legacy-validator-regression
context_clarifier: legacy persona validator folders are unmoved and atdd validate
  behaviour is unchanged while the new suite runs in parallel
lens: functional.effectiveness
statement: minimize likelihood of legacy-validator-regression by ensuring legacy persona
  validator folders are unmoved and atdd validate behaviour is unchanged
acceptances:
- identity:
    urn: acc:validate-conventions:E005-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E005 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
