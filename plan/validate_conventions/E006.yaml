urn: wmbt:validate-conventions:E006
step: execute
direction: minimize
dimension: likelihood
object_of_control: unaccounted-legacy-validator
context_clarifier: every file under the four legacy validator roots appears in the
  parity map entries or the exclusion table
lens: functional.effectiveness
statement: minimize likelihood of unaccounted-legacy-validator by ensuring every file
  under the four legacy validator roots appears in the parity map entries or the exclusion
  table
acceptances:
- identity:
    urn: acc:validate-conventions:E006-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E006 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
