urn: wmbt:validate-conventions:E029
step: execute
direction: minimize
dimension: likelihood
object_of_control: unclassified-catch-outcome
context_clarifier: each fault yields a catch-matrix cell of both, convention-only,
  legacy-only, or neither
lens: functional.effectiveness
statement: minimize likelihood of unclassified-catch-outcome by assigning each fault
  a catch-matrix cell of both, convention-only, legacy-only, or neither
acceptances:
- identity:
    urn: acc:validate-conventions:E029-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E029 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
