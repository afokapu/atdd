urn: wmbt:validate-conventions:E010
step: execute
direction: minimize
dimension: likelihood
object_of_control: unbuilt-p0-variant
context_clarifier: every P0 direct/split/merged/superseded entry from the parity map
  has a target implementation or explicit superseded evidence
lens: functional.effectiveness
statement: minimize likelihood of unbuilt-p0-variant by ensuring every P0 direct,
  split, merged or superseded parity entry has a target implementation or explicit
  superseded evidence
acceptances:
- identity:
    urn: acc:validate-conventions:E010-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E010 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
