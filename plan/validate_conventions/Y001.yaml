urn: wmbt:validate-conventions:Y001
step: modify
direction: minimize
dimension: likelihood
object_of_control: unsafe-legacy-deletion
context_clarifier: no legacy validator is deleted unless its parity status is direct,
  merged, split or superseded and its target implementation exists
lens: functional.effectiveness
statement: minimize likelihood of unsafe-legacy-deletion by refusing to delete any
  legacy validator unless its parity status is direct, merged, split or superseded
  and its target implementation exists
acceptances:
- identity:
    urn: acc:validate-conventions:Y001-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the Y001 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
