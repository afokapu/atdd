urn: wmbt:validate-conventions:E021
step: execute
direction: minimize
dimension: likelihood
object_of_control: untemplated-failure-evidence
context_clarifier: each variant emits evidence whose keys are a subset of its template
  failure_evidence
lens: functional.effectiveness
statement: minimize likelihood of untemplated-failure-evidence by requiring each variant's
  evidence keys to be a subset of its template failure_evidence
acceptances:
- identity:
    urn: acc:validate-conventions:E021-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E021 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
