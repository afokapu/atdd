urn: wmbt:validate-conventions:E003
step: execute
direction: minimize
dimension: likelihood
object_of_control: incomplete-template-contract
context_clarifier: template_contract.py defines all eight mandatory template metadata
  fields
lens: functional.effectiveness
statement: minimize likelihood of incomplete-template-contract by ensuring _support/template_contract.py
  defines all eight mandatory template metadata fields
acceptances:
- identity:
    urn: acc:validate-conventions:E003-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E003 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
