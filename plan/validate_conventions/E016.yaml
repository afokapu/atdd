urn: wmbt:validate-conventions:E016
step: execute
direction: minimize
dimension: likelihood
object_of_control: unresolved-p0-shadow-gap
context_clarifier: the shadow-run report shows zero unresolved P0 parity gaps before
  promotion
lens: functional.effectiveness
statement: minimize likelihood of unresolved-p0-shadow-gap by ensuring the shadow-run
  report shows zero unresolved P0 parity gaps before promotion
acceptances:
- identity:
    urn: acc:validate-conventions:E016-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E016 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
