urn: wmbt:validate-conventions:E012
step: execute
direction: minimize
dimension: likelihood
object_of_control: unbuilt-p1-variant
context_clarifier: every P1 parity entry is implemented or explicitly deferred with
  a recorded reason
lens: functional.effectiveness
statement: minimize likelihood of unbuilt-p1-variant by ensuring every P1 parity entry
  is implemented or explicitly deferred with a recorded reason
acceptances:
- identity:
    urn: acc:validate-conventions:E012-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E012 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
