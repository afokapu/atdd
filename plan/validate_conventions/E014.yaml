urn: wmbt:validate-conventions:E014
step: execute
direction: minimize
dimension: likelihood
object_of_control: parallel-run-collision
context_clarifier: target convention validators and legacy persona validators run
  in parallel without collision
lens: functional.effectiveness
statement: minimize likelihood of parallel-run-collision by ensuring target convention
  validators and legacy persona validators run in parallel without collision
acceptances:
- identity:
    urn: acc:validate-conventions:E014-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E014 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
