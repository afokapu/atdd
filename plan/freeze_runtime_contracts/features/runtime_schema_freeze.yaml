urn: "feature:freeze-runtime-contracts:runtime-schema-freeze"
wagon: "wagon:freeze-runtime-contracts"
description: "Freeze the coach v9 runtime + validator contracts: six JSON schemas with fixtures (runtime-event, coach-decision, coach-judgment, correction, validator-result, risk-score) plus three contract docs (runtime-layout.md, validator-invocation.md, event-semantics.md). Unblocks six parallel coach v9 tracks (J/K/L/M/N/O/P) by eliminating cross-interface merge conflicts."
sizing:
  wmbts: 4
  footprint_score: 6
  footprint_size: "S"
wmbts:
  - "wmbt:freeze-runtime-contracts:D001"
  - "wmbt:freeze-runtime-contracts:D002"
  - "wmbt:freeze-runtime-contracts:D003"
  - "wmbt:freeze-runtime-contracts:D004"
components:
  backend:
    presentation:
      - type: "controllers"
        count: 0
        rationale: "Documentation- and schemas-only PR; no runtime entry points"
    application:
      - type: "use_cases"
        count: 0
        rationale: "No application-layer logic introduced; downstream tracks (M, N, O) consume the schemas"
    domain:
      - type: "value_objects"
        count: 6
        rationale: "Each schema (runtime-event, coach-decision, coach-judgment, correction, validator-result, risk-score) is a value-object contract whose shape is frozen here"
    integration:
      - type: "schemas"
        count: 6
        rationale: "Six JSON Schema files committed at src/atdd/coach/schemas/ with example fixtures"
      - type: "docs"
        count: 3
        rationale: "runtime-layout.md, validator-invocation.md, event-semantics.md — semantic contracts that complement the schemas"
