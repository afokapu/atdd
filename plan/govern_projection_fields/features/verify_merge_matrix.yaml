components:
  backend:
    application:
    - count: 1
      rationale: enumerate_matrix_cells() over declared merge rules crossed with the
        four divergence cases
      type: use_cases
    domain:
    - count: 1
      rationale: MatrixCellExercised specification asserting each cell asserts a merged
        result or a conflict
      type: specifications
    integration:
    - count: 1
      rationale: matrix fixture reader collecting the exercised (merge rule, divergence
        case) pairs from the test suite
      type: repositories
description: 'The merge-driver test matrix and its completeness check: every merge
  rule declared by the policy is exercised against identical, no-op, evidence-backed,
  and unsafe divergence.'
sizing:
  footprint_score: 5
  footprint_size: S
  wmbts: 1
urn: feature:govern-projection-fields:verify-merge-matrix
wagon: wagon:govern-projection-fields
wmbts:
- wmbt:govern-projection-fields:C002
