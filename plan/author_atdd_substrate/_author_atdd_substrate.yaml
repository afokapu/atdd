wagon: author-atdd-substrate
urn: "wagon:author-atdd-substrate"
name: "Author ATDD Substrate"
description: "First-class authoring substrate for ATDD governance artifacts: a shared `atdd author` CLI validation spine plus deterministic writers for the four canonical artifact kinds. One kind is conflict-free by construction (convention-node: one file per rule_id); the other three are registry-class (relationship, scope, gate) where many entries accumulate in one file. Each writer enforces a single canonical schema and flat canonical home per kind and performs dedup-insert + stable-sort + atomic write; for the registry kinds a re-sort/dedup git merge driver resolves residual concurrent-insert conflicts deterministically (it does not make them conflict-free). Authored artifacts pass normal ATDD validation unmodified."
theme: commons

subject: "agent:planner"
context: "authoring-new-governance-artifacts, concurrent-waves-insert-into-shared-registries"
action: "defines one canonical schema and flat canonical home per artifact kind, validates role/id/path on a shared author spine, and writes each artifact via dedup-insert + stable-sort + atomic write backed by a re-sort/dedup merge driver"
goal: "make it impossible to author a malformed or non-deterministically-ordered governance artifact, and deterministically resolve (not eliminate) merge collisions when many wagons insert into the same registry file concurrently"
outcome: "schema-valid convention-nodes, relationships.yaml, scopes.yaml, and per-trigger gate files authored deterministically through `atdd author`, all passing unmodified through normal ATDD validation"

features:
  - urn: "feature:author-atdd-substrate:substrate-spine"
  - urn: "feature:author-atdd-substrate:author-merge-driver"
  - urn: "feature:author-atdd-substrate:author-convention-node"
  - urn: "feature:author-atdd-substrate:author-relationship"
  - urn: "feature:author-atdd-substrate:author-scope"
  - urn: "feature:author-atdd-substrate:author-gate"
  - urn: "feature:author-atdd-substrate:package-composition"
  - urn: "feature:author-atdd-substrate:author-issue-body"

produce:
  - name: commons:author:substrate-schemas
    contract: null
    telemetry: null
    to: external
  - name: commons:author:author-spine
    contract: null
    telemetry: null
    to: external
  - name: commons:author:author-merge-driver
    contract: null
    telemetry: null
    to: external
  - name: commons:author:convention-node-command
    contract: null
    telemetry: null
    to: external
  - name: commons:author:relationship-command
    contract: null
    telemetry: null
    to: external
  - name: commons:author:scope-command
    contract: null
    telemetry: null
    to: external
  - name: commons:author:gate-command
    contract: null
    telemetry: null
    to: external
  - name: commons:author:issue-body-command
    contract: null
    telemetry: null
    to: external

consume: []

wmbt:
  total: 27
  D001: "maximize likelihood of schema-valid substrate by defining one canonical schema and flat canonical home per artifact kind and freezing the controlled vocabularies"
  C009: "minimize likelihood of orphan-extension-convention-nodes by asserting in atdd validate package that every convention node an extension owns is an endpoint of at least one edge in the extension's relationships.yaml"
  C008: "minimize likelihood of orphan-convention-nodes by asserting every convention node appears as a source_ref or target_ref of at least one relationship-graph edge (no node outside the graph), making graph integrity bidirectional"
  C001: "minimize likelihood of the shared atdd author spine accepting an invalid role, id, or path"
  P001: "minimize likelihood of wrong-authoring-context by resolving extension-vs-core (extension default; core only with --core)"
  P002: "maximize likelihood of a well-formed package-skeleton by scaffolding a new extension or workspace-provider package (manifest + canonical skeleton) from a validated namespaced id via `atdd author … init`"
  C006: "minimize likelihood of accepting a malformed package manifest (extension/workspace/implementation) or an incompatible provider↔implementation contract_version"
  K001: "minimize likelihood that authored artifacts are rejected or regressed by existing ATDD gates (authoring does not bypass validation)"
  R001: "minimize likelihood of an unresolved registry merge conflict by shipping a .gitattributes-registered re-sort/dedup git merge driver"
  E001: "maximize likelihood of schema-valid convention-node creation under a flat per-role nodes/ file"
  C002: "minimize likelihood of accepting a structurally invalid convention-node"
  E002: "maximize likelihood of a schema-valid, deterministically-ordered relationships.yaml when authoring an edge"
  C003: "minimize likelihood of accepting a malformed relationship edge"
  E003: "maximize likelihood of a schema-valid per-file scope (scopes/<scope_id>.scope.yaml) with embedded selectors sorted by selector_id"
  C004: "minimize likelihood of accepting a malformed or empty scope"
  E004: "maximize likelihood of a schema-valid, deterministically-ordered per-trigger gate file when authoring a gate entry"
  C005: "minimize likelihood of accepting a malformed gate"
  C007: "minimize likelihood of accepting an invalid package realization by validating realizes mappings (owned extension_node, shipped core_node, no design_candidate realized, derived targets) and refusing authored cross-package graph edges"
  E005: "maximize likelihood of a faithful composed protocol view by materializing realizes as derived edges with the provenance triple in composed mode only, source-only in core mode, never executing runtime"
  E006: "maximize likelihood of schema-valid issue-body creation when authoring from issue.schema.json (Graph Context + Mirror present, zero placeholders)"
  E007: "maximize likelihood of schema-valid convention-node validation metadata when authoring a convention node (optional block, family/(family,template) checked against registry.yaml, invalid JSON rejected, no concrete runtime state) without changing bind_rule semantics"
  C010: "minimize likelihood of accepting an invalid issue body (missing required section / missing Graph Context / out-of-enum Status) by validating against issue.schema.json instead of greping strings"
  C011: "minimize likelihood of schema-generator-gate drift by asserting issue.schema.json properties == create_issue_body fields == E019 required sections"
  K002: "minimize likelihood of existing-compliant-body validation regression by asserting today's compliant issue bodies validate against issue.schema.json (back-compat)"
  E008: "maximize likelihood of issue-publish-to-store when authoring via `atdd author issue` (store-first work_item + exactly one github external_ref, by default)"
  C012: "minimize likelihood of author-without-publish by requiring the generate path to reach the store-first publish and to fail loud (no body-only degrade) when the store is unreachable"
  C013: "minimize likelihood of test-induced-live-state-store-pollution by neutralizing the ATDD_CONTROL_ROOT override for the session, refusing in-process opens of the production store, and fingerprinting that store around every test and around the session"
