urn: wmbt:project-shared-state:E002
step: execute
direction: maximize
dimension: likelihood
object_of_control: projection-hydration
context_clarifier: when hydrate(projection) rebuilds public store objects from the committed per-uid YAML
  at HEAD, it must run with zero providers registered and no committed sqlite store on disk
lens: functional.effectiveness
statement: maximize likelihood of a lossless projection-hydration that rebuilds the public store objects
  from the committed projection at HEAD with no provider registered and no committed sqlite store
acceptances:
- given:
    abstract:
    - A directory of committed .atdd/state/projection/<uid>.yaml files for two objects
  harness:
    category: backend
    type: unit
  identity:
    id: AC-UNIT-001
    phase: RED
    purpose: hydrate(projection) reconstructs every public object field carried by the projection documents
    urn: acc:project-shared-state:E002-UNIT-001-rebuilds-objects-from-projection
  metadata:
    author: atdd:self-compliance
    created: '2026-07-09'
  then:
    abstract:
    - Both objects exist in the store keyed by uid
    - uid, slug, phase, body, train, and wmbts round-trip with the values carried by the YAML
  when:
    abstract: hydrate(projection_dir, store=<empty tmp store>) is invoked
- given:
    abstract:
    - A projection directory whose documents carry external_refs, and a registry with zero sync providers
  harness:
    category: backend
    type: unit
  identity:
    id: AC-UNIT-002
    phase: GREEN
    purpose: hydration completes with zero sync providers registered and never reads external_refs for
      a lifecycle decision
    urn: acc:project-shared-state:E002-UNIT-002-hydrates-without-provider
  metadata:
    author: atdd:self-compliance
    created: '2026-07-09'
  then:
    abstract:
    - Hydration succeeds and no provider lookup is attempted
    - external_refs are carried verbatim into the store and never consulted to derive phase
  when:
    abstract: hydrate(projection_dir, store=<empty tmp store>) is invoked
- identity:
    urn: acc:project-shared-state:E002-SMOKE-001-projection-hydration
    id: AC-SMOKE-001
    phase: SMOKE
    purpose: 'End-to-end: projection-hydration holds against a real repo checkout, the installed atdd
      CLI, and a real .atdd/state/state.sqlite — no manual patching.'
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - A real repo checkout, the installed atdd CLI, and a real local State Store exercising projection-hydration
  when:
    abstract: the E002 behavior is driven end-to-end through the real CLI
  then:
    abstract:
    - projection-hydration is observed to hold end-to-end against real infrastructure, with no mocks or
      manual patching
  metadata:
    author: atdd:self-compliance
    created: '2026-07-10'
