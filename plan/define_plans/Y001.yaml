urn: wmbt:define-plans:Y001
step: modify
direction: minimize
dimension: likelihood
object_of_control: decision-bypassing-elicit
context_clarifier: atdd plan session decide --verdict keep|pivot|kill records the
  verdict via the actor-neutral elicit contract
lens: functional.effectiveness
statement: minimize likelihood of decision-bypassing-elicit by routing every keep/pivot/kill
  verdict through the elicit channel
acceptances:
- identity:
    urn: acc:define-plans:Y001-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the Y001 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
