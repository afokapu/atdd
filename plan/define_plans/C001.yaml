urn: wmbt:define-plans:C001
step: confirm
direction: minimize
dimension: likelihood
object_of_control: pre-confirm-authoring
context_clarifier: PlanSession.author refuses to write until the session has passed
  the Confirm gate
lens: functional.effectiveness
statement: minimize likelihood of pre-confirm-authoring by writing no artifact before
  the operator confirms the locked decomposition
acceptances:
- identity:
    urn: acc:define-plans:C001-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the C001 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
