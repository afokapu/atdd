urn: wmbt:define-plans:P001
step: prepare
direction: minimize
dimension: likelihood
object_of_control: unit-without-author-spec
context_clarifier: atdd plan session unit --kind --ref --spec rejects a unit whose
  spec is not a valid atdd author input dict
lens: functional.effectiveness
statement: minimize likelihood of unit-without-author-spec by requiring each Prepare
  unit to carry kind, ref, and a valid atdd author spec
acceptances:
- identity:
    urn: acc:define-plans:P001-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the P001 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
