urn: wmbt:define-plans:D001
step: define
direction: minimize
dimension: likelihood
object_of_control: session-without-main-job
context_clarifier: atdd plan session advance --step locate is refused until the main-job
  (Define) is set on the session
lens: functional.effectiveness
statement: minimize likelihood of session-without-main-job advancing past Define by
  requiring a JTBD main-job before the Locate gate opens
acceptances:
- identity:
    urn: acc:define-plans:D001-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the D001 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
