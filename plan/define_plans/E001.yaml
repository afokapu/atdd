urn: wmbt:define-plans:E001
step: execute
direction: minimize
dimension: likelihood
object_of_control: author-dispatch-skip
context_clarifier: atdd plan session author writes exactly one artifact per kept unit
  through the atdd author writer
lens: functional.effectiveness
statement: minimize likelihood of author-dispatch-skip by invoking atdd author per
  locked unit on Confirm
acceptances:
- identity:
    urn: acc:define-plans:E001-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the E001 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
