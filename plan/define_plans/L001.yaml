urn: wmbt:define-plans:L001
step: locate
direction: minimize
dimension: likelihood
object_of_control: unbound-source-capture
context_clarifier: atdd plan session source <text> appends to the session sources
  and survives a reload
lens: functional.effectiveness
statement: minimize likelihood of unbound-source-capture by binding every source captured
  in Locate to the persisted session state
acceptances:
- identity:
    urn: acc:define-plans:L001-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the L001 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
