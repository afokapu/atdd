urn: wmbt:define-plans:C002
step: confirm
direction: minimize
dimension: likelihood
object_of_control: step-advance-without-exit-condition
context_clarifier: atdd plan session advance --step X is refused unless step X prerequisites
  are satisfied; backtracking remains allowed
lens: functional.effectiveness
statement: minimize likelihood of step-advance-without-exit-condition by advancing
  a step only when its exit condition holds
acceptances:
- identity:
    urn: acc:define-plans:C002-SMOKE-001-seed
    id: AC-SMOKE-001
    purpose: Verify the C002 behaviour against the real CLI in a checkout
    phase: SMOKE
  harness:
    type: integration
    category: backend
  given:
    abstract:
    - a real repo checkout and the installed atdd CLI
  when:
    abstract: the behaviour is exercised end-to-end
  then:
    abstract:
    - the observable outcome holds
